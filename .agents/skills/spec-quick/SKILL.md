---
name: spec-quick
description: Run the full spec workflow (requirements → design → tasks → implementation) end-to-end WITHOUT approval gates, for small, well-understood features. Use when specs should stay the source of truth but you do not need to review each phase.
---

This is the quick phase of the spec-driven workflow.

Read and follow the canonical procedure in `.claude/skills/spec-quick/SKILL.md` (the
authoritative steps) together with `.ai/shared/TASK_PROTOCOL.md` and the relevant
`.ai/workflows/feature-development.md`.

This is the one exception to the approval gates: run requirements -> design -> tasks ->
implementation end-to-end without stopping, but keep the specs as the source of truth.
