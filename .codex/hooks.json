{
  "_note": "NOT LOADED BY CODEX. Codex discovers hooks from .codex/config.toml under the [hooks] table (sourcePath is always config.toml), not from a standalone hooks.json. This file is kept only as a cross-harness reference mirroring the Claude hook shape; the LIVE, enforced wiring lives in .codex/config.toml [hooks]. Do not rely on this file for enforcement. See .ai/agents/codex/AGENT.md (Pre-tool guard / Task-gate).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          { "type": "command", "command": ".codex/hooks/guard.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^(apply_patch|Bash|Write|Edit)$",
        "hooks": [
          { "type": "command", "command": ".codex/hooks/task-gate.sh", "timeout": 120 }
        ]
      }
    ]
  }
}
