# Review record — PR #102 @ 1034446

> sdd-premerge-review-standard — one file per reviewed PR head, keyed by head sha (not
> PR number alone: squash-merge makes PR titles/numbers an unreliable "what shipped"
> proxy, LESSONS.md `squash-merge-title-unreliable`). A record's `<sha7>` must equal the
> PR's CURRENT `headRefOid` prefix; a stale-head record does not satisfy the gate —
> write a new one (or update this one, renamed) after any post-review fix commit.

- date: 2026-07-11
- kind: review-fanout
- finders/verifiers: 11 finders (10 angles + 1 gap sweep) / 8 verifiers — 19 agents total
- override reason: —

Reviewed head: `181564f` (run wf_9c10d3d2-772, 21 candidates -> 6 dedup + 2 sweep -> 6
CONFIRMED / 2 REFUTED). Fixes for all 6 confirmed findings landed as `1034446` on the
same PR branch; this record is keyed to that fix commit per the staleness rule
(re-review scope: fix re-verified by the full suite — 301/301 — plus new e2e pins for
every confirmed failure mode and the 16-file `satisfies_text` byte-identity harness;
no fresh fan-out spent on the fix commit — operator-approved single review round).

| # | finding (file:line) | verdict | outcome |
|---|---|---|---|
| 1 | scripts/spec_to_goal.py:91 — Evidence cut patched in one consumer (task_maps) while sibling consumer satisfies_text (CI coverage gate) still parses transcripts; right altitude = shared iter_task_blocks | CONFIRMED | fixed (line-anchored cut moved into iter_task_blocks; task_maps substring cut removed) |
| 2 | .ai/specs/platform-phase5-stage1/design.md:94 — "ไม่มีทาง fake-resolve" overclaim: substring cut had two resolved-with-wrong-value paths (mid-command `Evidence:` truncation; pre-Satisfies `Evidence:` collapsing double-cover) | CONFIRMED | fixed (mechanism + wording rewritten; both paths now impossible by construction and pinned by e2e) |
| 3 | scripts/spec_trace.py:159 — consumer drift: spec-trace gate goes green on coverage that exists only in an Evidence transcript (Satisfies-last block) | CONFIRMED | fixed (shared cut; new e2e proves the gate now exits 1 on transcript-only coverage) |
| 4 | console/backend/src/spec-to-goal.e2e.test.ts:305 — archive assertion unfalsifiable (`!includes('Evidence:')` on a value that is always a prefix of the cut) and resolved/unresolved split unpinned | CONFIRMED | fixed (archive case pins `126 resolved / 0 unresolved` + no pending block) |
| 5 | scripts/spec_to_goal.py:91 — cut case-sensitive while floor engine `.ai/bin/check-evidence.sh` certifies Evidence headers case-insensitively | CONFIRMED | fixed (cut matches the floor's case-insensitive header definition; lowercase `evidence:` pinned by e2e) |
| 6 | console/backend/src/spec-to-goal.e2e.test.ts:84 — no test pinned the transcript-quoting-markers protection claim | CONFIRMED | fixed (fixture transcript now quotes `Satisfies:`/`Verify:`; mutant that only truncates the Verify segment now fails tests) |
| 7 | console/backend/src/spec-to-goal.e2e.test.ts:301 — `?? []` allegedly narrowed validation on the archive path | REFUTED | rejected (archive path never reaches the empty-array gate; bare-key regression pinned loudly by the unresolved-branch tests) |
| 8 | .ai/specs/platform-phase5-stage1/design.md:91 — amendment rationale allegedly duplicated 3x in one file | REFUTED | rejected (status line is mandated convention, Deviations log is append-only provenance; single normative copy remains) |
