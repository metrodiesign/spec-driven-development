# Review record — PR #119 @ 2f8982c

> sdd-premerge-review-standard — one file per reviewed PR head, keyed by head sha (not
> PR number alone: squash-merge makes PR titles/numbers an unreliable "what shipped"
> proxy, LESSONS.md `squash-merge-title-unreliable`). A record's `<sha7>` must equal the
> PR's CURRENT `headRefOid` prefix; a stale-head record does not satisfy the gate —
> write a new one (or update this one, renamed) after any post-review fix commit.

- date: 2026-07-12
- kind: review-fanout
- finders/verifiers: 11 finders (10 angles + 1 gap sweep) / 15 verifiers — 26 agents total
- override reason: —

Reviewed head: `427b57c` (run wf_44c2235b-515, 15 candidates -> 14 dedup + 1 sweep -> 12
CONFIRMED/PLAUSIBLE / 2 REFUTED). Fixes for 8 of the 12 findings landed as PR #120 (5
commits, squash-merged as `2f8982c` onto this same PR branch); 4 findings (#7, #8, #9,
#10) were intentionally left unfixed with reasoning documented in PR #120's body (perf
deferred / REQ-3.11 decision log explicitly rejected the shared-helper extraction these
would require) — see outcomes below. This record is keyed to the fix commit per the
staleness rule. A follow-up Codex automated review (PR #119 review 4680017153)
independently raised the same issue as finding #3 against the pre-fix code; verified
already covered by the same fix, no additional change needed (reply:
PR #119 comment #4951394257).

| # | finding (file:line) | verdict | outcome |
|---|---|---|---|
| 1 | scripts/spec-slice.sh:196 — dedup cache keyed off SECTION_VAL before heading resolution, so a second REQ row sharing an unresolvable Section value gets silently dropped instead of its own MISSING marker | CONFIRMED | fixed (cache write moved to after `find_heading_line()` succeeds) |
| 2 | scripts/spec-slice.sh:58 (+44, 68, 149) — unclosed fence never re-closes, so the boundary check can't fire again and the next real section is silently merged in, exit 0, no MISSING | CONFIRMED | fixed (all 4 sites print a shared `FENCE_UNCLOSED_MARK` sentinel from an `END` block; callers turn it into a loud MISSING/ERROR) |
| 3 | scripts/spec-slice.sh:157 — HEADER_ROW/data-row bash loops have no fence-tracking, so a fenced example table before the real Requirement Traceability table gets read as the real header/data | CONFIRMED | fixed (TRACE_BLOCK-building awk now gates `print` on `!fence`, so fenced pipe rows never reach the consumer loops at all) |
| 4 | .claude/skills/spec-design/SKILL.md:40 — `/spec-design` template never updated for the Section column this PR's matcher requires; every future spec generated from it would regress | CONFIRMED | fixed (template now documents the 3-column shape + exact-heading-text contract) |
| 5 | scripts/spec-slice.sh:84 — `header_col()` off-by-one (`i < NF`) skips the last column when the header row has no trailing pipe (valid GFM) | CONFIRMED | fixed (`i <= NF`) |
| 6 | scripts/spec-slice.sh:67 — `find_heading_line()` compares via `awk -v want=...`, which escape-processes backslash sequences on one side only | CONFIRMED | fixed (value threaded through `ENVIRON` instead of `-v`, both `find_heading_line()` and `section_from_heading()` — the latter inherits the same value one call later) |
| 7 | scripts/spec-slice.sh:198 — `find_heading_line()`/`section_from_heading()` each rescan design.md from line 1 per distinct Section value (O(2K) full-file scans) | CONFIRMED | accepted (perf-only, deferred — reasoning in PR #120 body) |
| 8 | scripts/spec-slice.sh:42 — fence-aware boundary-scan state machine triplicated across req_block/section_from_heading/TRACE_BLOCK awk | CONFIRMED | accepted (REQ-3.11 decision log explicitly chose 3 independent scans over a shared helper; not extracted) |
| 9 | scripts/spec-slice.sh:44 — fence-guard + trim pattern hand-copied across 3-4 sites | CONFIRMED | accepted (same reasoning as #8; correctness fix from #2 applied identically at all sites instead) |
| 10 | scripts/spec-slice.sh:148 — TRACE_BLOCK extraction re-implements `section_from_heading()` instead of calling it | CONFIRMED | accepted (delegating would face the same REQ-3.11 decision-log tension as #8/#9; also this finding's own verification was flagged low-confidence) |
| 11 | scripts/spec-slice.sh:121 — `DESIGN_ELEMENTS_SEEN` name stale after the diff retired the mechanism it referred to (now holds Section-column values) | CONFIRMED | fixed (renamed to `SECTION_VALS_SEEN` + comment, folded into #1's commit) |
| 12 | scripts/spec-slice.sh:164 — header-row detection changed content-based -> positional, no test covers a table with a second header-shaped row | PLAUSIBLE | fixed (regression test added; production logic unchanged — both adversarial fixtures degrade safely) |
| 13 | scripts/spec-slice.sh:86 — `header_col()` re-implements `cell_at()`'s trim logic instead of driving it | REFUTED | rejected (claimed silent-failure impact is actually a loud MISSING + REQ-4.2 fallback; duplication real but harmless) |
| 14 | scripts/spec-slice.sh:155 — TRACE_BLOCK "iterated twice" (throwaway header pre-pass + main loop) | REFUTED | rejected (pre-pass loop `break`s on the first match — O(1), not O(n); the claimed 2x cost doesn't hold) |
