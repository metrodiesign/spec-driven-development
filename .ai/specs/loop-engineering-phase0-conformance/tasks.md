# Implementation Tasks: Loop Engineering Phase 0 Conformance

> Status: approved 2026-07-27, amended 2026-07-27

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass, starting with a failing test/fault scenario and ending with GREEN
> evidence. Do not split these entries into numbered subtasks.

- [x] 1. P0-01 Pin the normative source byte-for-byte — copy `/Users/king_developer/Downloads/loop-engineering-implementation-spec.md` to repository-root `loop-engineering-implementation-spec.md` without changing one byte; block later tasks unless both `cmp` and SHA-256 match; leave every old master/blueprint/archive untouched.
  Satisfies: REQ-1 (all criteria). Verify: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` and `shasum -a 256 loop-engineering-implementation-spec.md` must report `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`; review `git diff --name-only` for no old-authority edit.
  Evidence:
    - satisfies: REQ-1.1–REQ-1.7
    - test: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md; cmp_status=$?; printf 'cmp_exit=%s\n' "$cmp_status"` -> `cmp_exit=0`
    - test: `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3  loop-engineering-implementation-spec.md`
    - test: `wc -c -l loop-engineering-implementation-spec.md` -> `345` lines, `41435` bytes
    - test: `git diff --check` -> exit `0`, no output
    - changed paths: `git status --short --untracked-files=all` -> root authority is the only new repository-root artifact; no old master, blueprint, archive, production, or test path listed; existing active-spec artifacts remain untracked and preserved
    - viewports: n/a — documentation-only
    - deviations: none

- [x] 2. P0-02 Unify fail-closed child-process execution — close every unresolved Task 08 boundary finding; expose only one core-owned `RUN_COMMAND` entry that derives trusted mode/role/roots/input hash, keeps low-level sandbox spawn module-private, and owns the complete disposable materialize→spawn→two-inventory capture→policy→promote→cleanup lifecycle; content-bind tool identity; treat deprecated macOS sandboxing as inherited filesystem/network effect denial rather than universal attempt audit; publish typed signal/denial/capability evidence; reject every Phase 0 network grant; production-wire the minimal role-scoped offline dependency policy (exact lockfile, approved source hashes, lifecycle scripts disabled, `network: none`); preserve read-only test/probe result semantics; record new-session CPU/cleanup escape as an explicit residual without weakening protected artifact/network guarantees.
  Satisfies: REQ-2 (all criteria). Depends on: 1. Verify: targeted low-level spawn, core command executor, gate runner, production composition, and wired fault tests prove raw spawn/mode/roots/input-hash bypass is impossible; fixed/config-isolated child tooling and same-path toolchain byte replacement affect `environmentHash`; direct enforcement observation produces `sandbox_violation`; ordinary signal is typed without false attribution; a normal non-zero exit after partial writes becomes `command_failed`, promotes nothing, and cleans its workspace; child output cannot fake attribution; a parent-swallowed descendant denial records `observedViolation:null`, the enforced policy hash, `direct_only`, containment `false`, and `unproven_new_session` while its unauthorized effect remains absent; macOS network-allow requests fail before spawn; offline install works only for a permitted role under production-wired exact-lockfile/content-hash policy with lifecycle scripts disabled and `network: none`; missing/tampered lockfile, unapproved source, script hook, or unauthorized role fails before package spawn; pre/post source inventories plus capture hashes must match before exact frozen-diff promotion; churn, ignored residue, unsupported shapes, each capture failpoint, and every injected file/byte/diff/time bound promote nothing and clean all core-owned temporaries; delayed new-session writes stay discarded; read-only tests/probes preserve exit/output evidence; original artifacts remain byte-identical except for the exact core-promoted diff.
  Review status: reopened by the approved 2026-07-27 amendment after Task 08 `REQUEST_CHANGES`; prior Evidence remains preserved verbatim below and is historical only, not proof that the amended criteria are complete.
  Evidence:
    - satisfies: REQ-2.1–REQ-2.12
    - RED executor/gate: `pnpm --filter core exec node --test --test-reporter spec src/executor/executor.test.ts src/gates/runner.test.ts` -> `22` tests, `14` pass, `8` fail on planner/test-designer command writes, shared package-install boundary, gate raw-IP boundary, disposable mutation, sandbox-unavailable gate, empty T0, and malformed config
    - RED wired faults: `pnpm --filter core exec node --test --test-reporter spec test/fault-injection.test.ts` -> `21` tests, `14` pass, `7` fail; new discriminating failures were DoD#2c, DoD#2d, DoD#3b, and DoD#3c (the other three were expected nested-sandbox refusals in existing command faults)
    - review RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'recovery rolls back partial allowed writes' src/executor/executor.test.ts` -> `1` failed because `src/partial.txt` remained after a sandbox-rejected replay; the same command passed after the recovery rollback fix
    - targeted real macOS sandbox: `pnpm --filter core exec node --test --test-reporter spec src/security/command-runner.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts test/fault-injection.test.ts` outside the managed sandbox -> `51` passed, `0` failed
    - full core: `pnpm --filter core test` outside the managed sandbox -> `300` passed, `0` failed
    - production composition regression: `pnpm --filter console-backend test` outside the managed sandbox -> `381` passed, `0` failed
    - typecheck: `pnpm --filter core typecheck` and `pnpm --filter console-backend typecheck` -> exit `0`
    - lint: `pnpm lint` -> `ESLint: No issues found`
    - vendor scan: `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`
    - diff hygiene: `git diff --check` -> exit `0`, no output
    - review: correctness and security review found and fixed one recovery partial-write rollback gap with a discriminating RED/GREEN regression; no unresolved actionable finding remains; output capture is bounded, child process groups are killed on timeout/cap, worktree checkout hooks are disabled, role/golden containment is enforced by macOS SBPL, and sandbox absence fails closed
    - changed paths: shared command primitive and tests; sandbox/profile, executor/path policy, gate runner, production composition, wired faults/fixture, plus OOB/deploy regression controls required by disposable gate semantics
    - dependencies: none added
    - viewports: n/a — core/backend execution behavior only
    - deviations: none; real `sandbox-exec` evidence was intentionally run outside the nested managed sandbox per D-003
    - review-fix RED: `pnpm --filter core exec node --test --test-reporter spec src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts` -> `57` tests, `30` pass, `27` fail before production changes; `22` failures discriminated the nine review findings and `5` were expected nested-sandbox refusals
    - review-fix focused GREEN: the same focused suite outside the managed sandbox -> `58` passed, `0` failed, including real macOS SBPL hidden-denial, path-encoding, protected-write, package-install, descendant-cleanup, and frozen-tree probes
    - review-fix full core: `pnpm --filter core test` outside the managed sandbox -> `325` passed, `0` failed
    - review-fix production composition: `pnpm --filter console-backend test` outside the managed sandbox -> `381` passed, `0` failed; the first managed-sandbox attempt failed only because that harness forbids the suite's loopback listener with `listen EPERM 127.0.0.1`
    - review-fix typecheck/lint: `pnpm --filter core typecheck`, `pnpm --filter console-backend typecheck`, and `pnpm lint` -> exit `0`
    - review-fix repository gates: `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `107` criteria covered and EARS pass; `git diff --check` -> exit `0`
    - review-fix closure: materialization now uses isolated Git plumbing without filters, hooks, caller index, object database, or registered worktrees; command and built-in checks share one frozen tree; enforcement-owned `SIGKILL` denial cannot be hidden in child output; recovery is bound to run/task/action and rejects missing or invalid roles; sanitized toolchain identity is hashed and governed installs get narrow ephemeral roots; process groups are terminated on every completion; timeout validation and cancellation fail closed; SBPL paths are encoded; mixed OOB hard failures take precedence over flakes
    - review-fix residual decision: a child that self-terminates with `SIGKILL` is conservatively rejected, and a descendant that deliberately creates a new session can escape process-group cleanup until OS/process exit although SBPL continues to constrain file and network access; these are fail-closed/availability residuals, not a false-success path
    - review-fix handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-07-p0-02-review-fixes.md`
  Amended implementation evidence (awaiting fresh review):
    - scope: implements the approved 2026-07-27 REQ-2.1–REQ-2.49 amendment only; Task 2 deliberately remains `[ ]` until an independent fresh-context review and real macOS verification complete
    - REQ-2.1 RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'executor, gate, and workspace preparation' src/security/amended-command-contract.test.ts` -> `1` test, `0` pass, `1` fail because `core/src/executor/executor.ts` still launched fixed Git tooling directly
    - REQ-2.1 GREEN: the same focused command after routing executor and frozen-tree Git launches through the internal shared child-process boundary -> `1` pass, `0` fail; final package-surface/boundary regression -> `2` pass, `0` fail and proves `createCommandRunner`, `runCoreTool`, and `denyNetworkSandbox` are not public `core` exports
    - amended contract plus frozen-tree matrix: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts` -> `23` tests, `22` pass, `0` fail, `1` explicit real-macOS skip
    - focused P0-02 regression: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts test/fault-injection.test.ts` -> `108` tests, `100` pass, `0` fail, `8` explicit external real-macOS skips
    - production policy/composition regressions: `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal exec node --test --test-reporter spec test/integration.test.ts` -> `3` pass, `0` fail
    - full core: `pnpm --filter core test` in the managed sandbox -> `350` tests, `341` pass, `0` fail, `9` explicit external real-macOS skips
    - full console backend: `pnpm --filter console-backend test` in the managed sandbox -> `383` tests, `329` pass, `54` environment failures; `53` are `listen EPERM: operation not permitted 127.0.0.1` and the remaining doctor assertion is caused by `node:os.uptime()` failing with `uv_uptime returned EPERM`, so no functional P0-02 failure was observed
    - typecheck: `pnpm --filter core typecheck`, `pnpm --filter aal typecheck`, and `pnpm --filter console-backend typecheck` -> exit `0`
    - repository gates: `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md` and `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness/security pass found no remaining actionable issue after the REQ-2.1 bridge fix; fresh-context review is still required and must scrutinize the single child boundary, package-private low-level bridge, disposable capture/promotion race controls, offline dependency policy, and typed evidence semantics
    - external blocker: the escalated real macOS run was not retried because the account usage limit remains active until August 2 at 11:46; managed-sandbox skips are not claimed as real macOS PASS
    - residual: a descendant that creates a new session may retain CPU/process lifetime beyond direct-child cleanup; inherited SBPL and disposable capture still deny unauthorized durable filesystem/network effects, with evidence reporting `direct_only`, containment `false`, and `unproven_new_session`
    - dependencies: none added
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-10-p0-02-amended-implementation.md`
  Amended review-fix evidence (awaiting fresh re-review):
    - scope/status: resolves Task 11 High findings H1–H3 against amended REQ-2.1–REQ-2.49 only; Task 2 deliberately remains `[ ]` pending independent fresh-context re-review and the blocked external real-macOS run
    - H1 RED: the focused non-empty/missing-source/frozen-revalidation cases initially produced `5` tests, `0` pass, `5` fail; a separate real non-empty offline install test produced `1` fail with `command_failed`, proving the vacuous policy had no usable source bytes or persistence
    - H1 GREEN: policy now binds an exact manifest hash, exact lockfile hash, non-empty package graph, core-owned read-only local source object and SHA-256 content hash, plus the sole persistent output root `node_modules`; core freezes before validating, provisions captured source bytes read-only, verifies installed package bytes against the approved source, rejects missing/tampered/unsupported graphs before or without spawn as appropriate, promotes approved output, and a later read-only command consumes it successfully
    - H2 GREEN: regular-file capture now traverses from a held root directory descriptor with `O_NOFOLLOW`, reads and hashes through one file descriptor, checks pre/post `fstat` identity, cross-checks streamed length/digest, and keeps the existing pre/post/exclusive-capture inventories; the adversarial symlink-swap regression rejects and cleans the whole attempt
    - H3 GREEN: command and fixed core tools now compose one internal async `spawn()` primitive with typed completion/signal/timeout/cancellation/output-limit/launch-failure results, bounded combined output, timeout/abort handling, process-group termination and bounded reap; executor/frozen-tree Git preparation is async and the architecture regression proves there is no second sync child implementation
    - focused final P0-02 regression: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts test/fault-injection.test.ts` -> `114` tests, `106` pass, `0` fail, `8` explicit external real-macOS skips
    - full core: `pnpm --filter core test` -> `356` tests, `347` pass, `0` fail, `9` explicit external real-macOS skips
    - production composition: `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; production fixture/policy graph is non-empty and source/hash-bound; `pnpm --filter aal exec node --test --test-reporter spec test/integration.test.ts` -> `3` pass, `0` fail; full AAL -> `143` pass, `0` fail
    - full console backend: `383` tests, `329` pass, `54` managed-sandbox environment failures; `53` are `listen EPERM: operation not permitted 127.0.0.1`, and the remaining doctor failure is reproduced by `node:os.uptime()` throwing `uv_uptime returned EPERM`; no P0-02 functional failure was observed
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint`, `scripts/check-core-vendor-free.sh`, and `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> pass, with `144` criteria covered and EARS lint passing
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: final correctness/security review found and fixed the need to verify installed dependency bytes against the approved source; no remaining actionable finding was identified in H1–H3 scope
    - external blocker: the real macOS suite was not retried or circumvented because the recorded account usage limit remains active until August 2 at 11:46; managed-sandbox skips are not claimed as a real-macOS PASS
    - dependencies/viewports: no package dependency added; descriptor-safe capture uses the content-hashed system `/usr/bin/python3` tool on the required macOS platform; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-12-p0-02-amended-review-fixes.md`
  Second review-fix evidence (awaiting fresh re-review):
    - scope/status: resolves Task 13 H1, M1, and M2 only against amended REQ-2.1–REQ-2.49; Task 2 deliberately remains `[ ]` pending independent fresh-context re-review and the blocked external real-macOS run
    - H1 RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'frozen installed bytes are revalidated' src/security/amended-command-contract.test.ts` -> `1` test, `0` pass, `1` fail because a stable mutation after live installed-byte verification was captured and promoted
    - H1 GREEN: the same focused command -> `1` pass, `0` fail; after pre/post/captured inventories match and the exclusive capture is immutable, core now compares the captured installed subtree's exact relative paths, modes, byte lengths, and SHA-256 digests with the approved source snapshot before diff or promotion
    - M1 RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'approved source capture obeys' src/security/amended-command-contract.test.ts` -> `4` tests, `0` pass, `4` fail across the parent plus file-count, total-bytes, and deadline cases because source preflight reached the later capture path
    - M1 GREEN: the same focused command -> `4` pass, `0` fail; approved-source capture and live/provisioned installed verification now use iterative traversal, aggregate finite `maxFiles`/`maxSingleFileBytes`/`maxTotalBytes` budgets, and a deadline started before approved-source capture, returning typed `preflight_rejected/offline_dependency_unavailable` with no spawn or temporary residue
    - M2 RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'evidence failures at wrap and post-child' src/security/command-runner.test.ts` -> `3` tests, `0` pass, `3` fail across the parent plus wrap and post-child throwing-evidence cases; each left one `command-scratch-*` directory
    - M2 GREEN: the same focused command -> `3` pass, `0` fail; one outer `try/finally` now owns the command scratch lifecycle and removes it even when wrap-stage or post-child evidence persistence throws
    - combined H1/M1/M2 plus lifecycle control: focused name-pattern regression across amended contract and command runner -> `11` tests, `11` pass, `0` fail, including existing capture/promotion failpoints and the real non-empty offline persistence control
    - focused final P0-02 regression: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts test/fault-injection.test.ts` -> `122` tests, `114` pass, `0` fail, `8` explicit external real-macOS skips
    - full core: `pnpm --filter core test` -> `364` tests, `355` pass, `0` fail, `9` explicit external real-macOS skips
    - production composition: `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal exec node --test --test-reporter spec test/integration.test.ts` -> `3` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md`, `.ai/bin/check-secrets.sh --all`, and `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness/security review found no unresolved actionable issue in H1/M1/M2 scope after the fixes; no dependency or policy change was required
    - external blocker: the real-macOS suite was not retried or circumvented because the recorded account usage limit remains active until August 2 at 11:46; managed-sandbox skips are not claimed as a real-macOS PASS
    - dependencies/viewports: none added; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-14-p0-02-second-review-fixes.md`
  Third review-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 15 H1 และ M1 ตาม amended REQ-2.1–REQ-2.49; Task 2 ยังคง `[ ]` เพื่อรอ independent fresh-context re-review และ external real-macOS run ที่ยัง blocked
    - H1 RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'exact captured installed set rejects' src/security/amended-command-contract.test.ts` -> `6` tests, `0` pass, `6` fail; stable sibling package, root file, manager metadata และ `.bin` entry ถูก promote ส่วน deleted approved file มี rejection detail ที่ยังไม่แสดง exact-set contract
    - H1 GREEN: command เดิม -> `6` pass, `0` fail; immutable verifier สร้าง exact installed-output path set จาก approved package files และ metadata ที่ policy enumerate เท่านั้น เปรียบเทียบ path/mode/bytes/SHA-256 ก่อน diff และให้ diff exception เฉพาะ exact set นั้น; regression เพิ่ม content validation ของ enumerated manager metadata -> `1` pass, `0` fail
    - M1 descriptor RED/GREEN: focused descriptor deadline/cancellation test เริ่มจาก `3` tests, `0` pass, `3` fail เพราะ helper ไม่รับ deadline/signal; หลังแก้ command เดิม -> `3` pass, `0` fail โดย 16 MiB read ถูก timeout ตาม remaining budget และ aborted read คืน typed cancellation ภายใน bounded elapsed time
    - M1 lifecycle RED/GREEN: cancellation matrix ที่ inject `AbortSignal` ระหว่าง freeze, materialize, spawn, inventories, copy, diff, evidence และ promote เริ่มจาก `11` tests, `0` pass, `11` fail เพราะทุก case ยัง promote; หลังส่ง monotonic operation control เดียวผ่าน traversal, descriptor/Git/core child, inventory, copy, evidence และ promotion -> `11` pass, `0` fail พร้อม typed `cancelled`, zero promotion และ attempt cleanup; tiny shared-deadline Git-preparation regression -> `1` pass, `0` fail พร้อม typed `timed_out`
    - production policy: `.ai/policies/security-plane.json` enumerate `.modules.yaml`, `.package-map.json`, workspace-state และ copied lock metadata พร้อม validator/version identity; production parser rejects missing, duplicate, escaping หรือ unknown metadata definitions และ content validators derive expected graph/settings from frozen inputs rather than trusting manager output
    - focused final P0-02 regression: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts test/fault-injection.test.ts` -> `144` tests, `136` pass, `0` fail, `8` explicit external real-macOS skips
    - full core/backend/AAL: `pnpm --filter core test` -> `386` tests, `377` pass, `0` fail, `9` explicit external real-macOS skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `.ai/bin/check-secrets.sh --all` and `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness/security review traced the exact-set exception, metadata trust boundary, one shared monotonic deadline, typed child outcomes, cleanup and rollback paths; no unresolved actionable finding remains in Task 15 H1/M1 scope
    - external blocker: ไม่ retry/circumvent external real-macOS suite เพราะ recorded account usage limit ยังมีผลถึง August 2 เวลา 11:46; managed-sandbox skips ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-16-p0-02-third-review-fixes.md`
  Fourth review-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 17 H1, M1 และ M2 ตาม amended REQ-2.1–REQ-2.49; Task 2 ยังคง `[ ]` เพื่อรอ independent fresh-context acceptance re-review และ external real-macOS run ที่ยัง blocked
    - initial RED: focused public-executor/FIFO/socket command ได้ `11` tests, `1` pass, `10` fail; ignored persistent output mutation/deletion/addition ไม่ถูกตรวจ, crash replay คง ignored residue, pre-abort/tiny deadline/abort-after-promotion ยังถึง inner executor หรือบันทึกผลสำเร็จ และ FIFO ถูกละเว้น; ignored socket rejection เป็น control เดียวที่ผ่านเดิม
    - H1 GREEN: `ACTION_APPLIED.resultHash` ใช้ content-addressed SHA-256 identity จาก exact frozen tree hash, inventory hash และ declared persistent roots; pre-intent snapshot เก็บ exact regular-file manifest/mode/content refs ของ owned ignored roots ใน EvidenceStore, rollback ลบและ restore เฉพาะ roots เหล่านั้น, recovery ตรวจ mutation/deletion/addition และ invalidates stale APPLIED state, duplicate ไม่รายงาน `skipped_duplicate` เมื่อ identity เสีย และ crash-after-promotion rollback ก่อน replay; rollback ตรวจ manifest/content blobs ครบก่อนเปลี่ยน owned root
    - M1 GREEN: frozen-tree preflight ใช้ fixed `/usr/bin/python3 -I` descriptor-relative `O_NOFOLLOW` traversal ที่ไม่พึ่ง Git path enumeration, ข้ามเฉพาะ repository `.git`, ตรวจทุก tracked/untracked/ignored object, reject FIFO/socket/device ทั้ง artifact และบังคับ `maxFiles`/`maxSingleFileBytes`/`maxTotalBytes` อิสระจาก Git inventory; traversal ถือ file descriptors ตาม directory depth และ cleanup ทุก failure
    - M2 GREEN: public `createExecutor` สร้าง monotonic operation control ก่อน outer snapshot แล้วส่ง remaining deadline และ AbortSignal เดียวผ่าน fixed Git/filter discovery, persistent snapshot, inner `CoreCommandExecutor`, descriptor/Git/materialize/capture/promotion และ final artifact identity; pre-abort ไม่มี `INTENT`, tiny deadline คืน typed `timed_out`, abort หลัง promotion ไม่มี `ACTION_APPLIED` และใช้ bounded core-only reconciliation restore snapshot
    - focused final P0-02 regression: `pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/gates/frozen-tree.test.ts src/security/command-runner.test.ts src/security/sandbox.test.ts src/executor/path-policy.test.ts src/executor/executor.test.ts src/gates/runner.test.ts src/audit/oob.test.ts test/fault-injection.test.ts` -> `158` tests, `150` pass, `0` fail, `8` explicit external real-macOS skips
    - full core/backend/AAL: `pnpm --filter core test` -> `400` tests, `391` pass, `0` fail, `9` explicit external real-macOS skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> exit `0`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `.ai/bin/check-secrets.sh --all` and `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness/security review traced persistent-manifest trust, path containment, exact root restoration, full-filesystem descriptor traversal, timeout/cancellation reason preservation, child cleanup and rollback; evidence prevalidation and traversal descriptor retention were hardened during review; no unresolved actionable finding remains in Task 17 H1/M1/M2 scope
    - external blocker: ไม่ retry/circumvent external real-macOS suite เพราะ recorded account usage limit ยังมีผลถึง August 2 เวลา 11:46; managed-sandbox skips ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency หรือ policy change ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-18-p0-02-fourth-review-fixes.md`
  Evidence:
    - satisfies: REQ-2.1–REQ-2.49
    - acceptance: fresh-context 3-lens review (correctness, security, concurrency/lifecycle) with per-finding adversarial verification -> `APPROVE` on all three lenses, `0` confirmed findings; a second adversarial pass over the remediation below also returned `APPROVE` with `0` actionable findings
    - blocker cleared: the external real-macOS run recorded as blocked since Task 07 was executed on 2026-08-02/03 outside the managed sandbox with `PHASE0_REAL_MACOS_TESTS=1`, which un-skips every probe that needs a real, non-nested `sandbox-exec`
    - first real-macOS run FAILED (4 regressions the managed sandbox had been hiding): `PHASE0_REAL_MACOS_TESTS=1 pnpm --filter core exec node --test --test-reporter spec src/security/amended-command-contract.test.ts src/security/command-runner.test.ts src/executor/executor.test.ts src/human/api.test.ts test/fault-injection.test.ts` -> `4` fail — DoD#2c, DoD#2d, and DoD#3 each got `command_failed` where `sandbox_violation` was required, and the real offline `package_install` died with `exit=null signal=SIGKILL`
    - root cause 1 (REQ-2.5 unimplemented on the real backend): `denyNetworkSandbox` never supplied `observeDirectResult`, so `source: 'enforcement_owned_direct'` had zero producers and every real SBPL denial degraded to an unattributed signal. Fix: the darwin backend now attributes a direct-child `SIGKILL` that core did not send as `{source:'enforcement_owned_direct', operation:'unknown'}` — a wait status is parent-owned kernel state, not child-controlled output, so REQ-2.22 permits it; core-initiated timeout/cancellation/output-limit kills are classified earlier and never reach the channel
    - root cause 2 (real offline install could not run at all): approved-source roots were `deny file-write*` with `send-signal SIGKILL`; the installer probes staging writes there, handles `EPERM`, but dies on `SIGKILL`. Fix: a policy-approved install demotes exactly those roots to a plain (killless) deny — they stay write-DENIED — and `profileFor` now refuses a graceful root that would contain a protected root. Empirically confirmed with `/usr/bin/sandbox-exec`: install succeeds, while writes to the source roots return `EPERM` with no bytes written and `test/golden` still SIGKILLs
    - rejected intermediate fix (recorded because it was live in-tree before review): an added `(allow network* (local unix))(allow network* (remote unix))` was REMOVED after adversarial review proved it was a real egress channel (DNS exfil through mDNSResponder and a working `/var/run/docker.sock` connect), and proved the IPC justification false — node socketpairs already work under the plain deny. Final profile grants no network relaxation of any kind; probes for DNS, raw-IP TCP, UDP, and unix-socket connect all return `rc=137` under the install profile
    - test-fixture defects found by the same run: the real install probe declared no `approvedOutputMetadata` (so pnpm's four metadata files were rejected as an unexpected output set) and `validatePnpmModulesMetadata` expected every package in `pendingBuilds`; the validator now derives that set from the frozen approved source bytes (`preinstall`/`install`/`postinstall` scripts or a root `binding.gyp`), which is fail-closed — a mismatch rejects, never accepts
    - evidence provenance: `typedEvidence` binds `offlineInstallProfile` per command, so an auditor can tell which command ran with the demoted source roots
    - regressions added: real SBPL direct-child kill -> `sandbox_violation`; a descendant denial the shell swallows -> `completed` with no attribution claim and the effect still denied (proves `direct_only` scope, REQ-2.23/2.37/2.40); graceful-root guard plus an assertion that the emitted profile contains no `allow network`
    - focused P0-02 GREEN: the same `PHASE0_REAL_MACOS_TESTS=1` command over amended contract, command runner, sandbox, executor, gate runner, frozen tree, OOB, and wired faults -> `252` tests, `252` pass, `0` fail, `0` skipped
    - full core GREEN: `PHASE0_REAL_MACOS_TESTS=1 pnpm --filter core test` -> `576` tests, `576` pass, `0` fail, `0` skipped — the `9` external-only skips recorded by every prior closure are now executed
    - full console backend: `pnpm --filter console-backend test` -> `394` tests, `394` pass, `0` fail; the `listen EPERM: operation not permitted 127.0.0.1` failures recorded since Task 07 were a managed-sandbox artifact and do not reproduce
    - full AAL: `pnpm --filter aal test` -> `148` pass, `0` fail
    - gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed
    - CI: the macOS platform job now exports `PHASE0_REAL_MACOS_TESTS=1`, so these enforcement probes run in the gate instead of skipping silently
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - residual (unchanged, explicitly recorded): a descendant that creates a new session can retain CPU/process lifetime past direct-child cleanup; denial-observation scope stays `direct_only`; a child that self-delivers `SIGKILL`, and an external/OOM kill, are rejected conservatively — fail-closed false positives, never a false success
    - dependencies/viewports: none added; viewports n/a — core execution behavior only
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-69-p0-02-close.md`

- [x] 3. P0-03 Complete `APPLY_PATCH` and document the `READ_FILE` exception — write patch/read lifecycle faults first, then add hash-verified Git-parsed text patches with all-path policy checks, structured unsupported/error cases, snapshot-intent-apply-applied recovery, duplicate handling, and non-mutating reads that record content refs without snapshot or intent.
  Satisfies: REQ-3 (all criteria). Depends on: 1. Verify: executor and wired fault tests cover valid add/update/delete/rename, missing/tampered/malformed/binary/symlink/traversal/golden/conflict/no-op patches, crash recovery, duplicate IDs, and exact `READ_FILE` event order.
  Implementation evidence (awaiting fresh review):
    - scope/status: implemented REQ-3.1–REQ-3.11 through the public `createExecutor` and durable event log; Task 3 remains `[ ]` pending an independent fresh-context review
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'APPLY_PATCH adds an allowed text file' src/executor/executor.test.ts` -> `1` test, `0` pass, `1` fail because the public executor returned `rejected` instead of `applied`
    - review RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'malformed, binary' src/executor/executor.test.ts` -> the `copy metadata` subtest failed because a `copy from test/golden/` patch was incorrectly applied; GREEN rejects copy metadata as `patch_unsupported` before mutation
    - APPLY_PATCH GREEN: evidence is dereferenced and hash-verified before Git inspection; Git owns syntax/applicability checks; supported add/update/delete/rename path discovery uses NUL-delimited raw output; every old/new path passes role, golden, lexical, and realpath policy before snapshot/intent; binary, symlink-bearing, copy, malformed, empty, conflicting, no-op, absolute, traversal, outside-role, missing, and tampered inputs return structured rejections
    - lifecycle/recovery GREEN: accepted patches follow snapshot -> `ACTION_INTENT` -> side effect -> `ACTION_APPLIED`; crash-after-intent and crash-after-apply restore the snapshot then replay once; repeated recovery is a no-op and duplicate action IDs skip without reapplying
    - READ_FILE GREEN: allowed reads use descriptor-relative `O_NOFOLLOW` plus before/after `fstat`, publish content-addressed `outputRef`/`resultHash`, emit only `ACTION_APPLIED`, and reject traversal or symlink escape without snapshot or intent
    - focused final: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'REQ-3' src/executor/executor.test.ts test/fault-injection.test.ts` -> `32` pass, `0` fail
    - full public/wired files: `executor.test.ts` -> `55` pass, `0` fail, `1` explicit external real-macOS skip; `fault-injection.test.ts` -> `18` pass, `0` fail, `5` explicit external real-macOS skips
    - full core/backend/AAL: `pnpm --filter core test` -> `432` tests, `423` pass, `0` fail, `9` explicit external real-macOS skips; relevant `console-backend` production composition -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> exit `0`; `scripts/check-core-vendor-free.sh` -> pass; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; strict Evidence, secret scan, and `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness/security review found and fixed descriptor-read TOCTOU exposure and copy-metadata source-path bypass; no unresolved actionable finding remains in P0-03 scope
    - external blocker: did not retry, circumvent, or claim PASS for the P0-02 external real-macOS suite; the recorded account usage limit remains active until August 2 at 11:46 Asia/Bangkok
    - dependencies/viewports: no dependency or policy change; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-20-p0-03-apply-patch.md`
  Review-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 21 High 1–3 และ Medium 1 บน public `createExecutor`/`recoverWorktree`; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'target-controlled Git|declared forbidden no-op|later same-action preflight|pre-aborted APPLY_PATCH' src/executor/executor.test.ts` -> `8` tests, `0` pass, `8` fail; clean/smudge marker ถูกเขียน, process filter ทำให้ patch reject, mixed forbidden no-op ทั้งสองแบบถูก apply, later same-action rejection ปิด dangling intent และ pre-aborted patch ยังทำ preflight ใช้เวลาประมาณ `786 ms`
    - High 1 GREEN: patch parse/check/apply ทุก Git call ใช้ fixed `/usr/bin/git` ผ่าน shared async core child พร้อม operation deadline/AbortSignal และ sanitized config; actual-worktree check/apply ใช้ temporary core-owned `GIT_DIR`, `GIT_INDEX_FILE` และ explicit `GIT_WORK_TREE` จึงไม่โหลด target local config; clean, smudge และ long-running process filter regressions ยืนยันว่าไม่มี external marker และ patch ยัง apply ได้
    - High 2 GREEN: Git parser สร้าง complete declared path set จาก NUL-delimited forward/reverse `git apply --numstat -z`, คืนลำดับ reverse ก่อนจับคู่ source/destination และตรวจทุก old/new path ก่อน mutation; effective raw diff ถูกคำนวณแยกเพื่อรองรับ add/update/delete/rename และจำแนก no-op/copy หลัง policy; mixed golden/role-disallowed no-op, quoted newline/tab/Unicode, rename สองทิศทาง และ copy-from-golden regressions ผ่านโดย inode/allowed path/INTENT ไม่เปลี่ยนเมื่อ reject
    - High 3 GREEN: mutating `ACTION_APPLIED` และ terminal `ACTION_REJECTED` หลัง intent มี immutable `intentSeq`; recovery ปิด dangling intent ได้เฉพาะ terminal event ที่อ้าง exact sequence เดียวกัน และหา applied snapshot ด้วย sequence แทน actionId; crash-after-apply ตามด้วย same-action preflight rejection จึง rollback/replay หนึ่งครั้ง, append recovered applied ที่ผูกกับ intent เดิม และ recovery ซ้ำเป็น no-op
    - Medium 1 GREEN: public operation control ถูกสร้างทันทีหลัง schema validation ก่อน duplicate/evidence/hash/parse/freeze/materialize/policy/check/snapshot/intent/apply/final identity; child timeout/cancellation ของ patch Git คืน typed `FrozenTreeOperationError`; pre-aborted regression ยืนยัน prompt `cancelled`, evidence read count `0`, ไม่มี filter/Git/snapshot/INTENT/APPLIED side effect และไม่มี worktree mutation
    - focused final: command initial RED เดิม -> `8` pass, `0` fail; `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'REQ-3' src/executor/executor.test.ts test/fault-injection.test.ts` -> `40` pass, `0` fail
    - full core/backend/AAL: `pnpm --filter core test` -> `440` tests, `431` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; strict Evidence, full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness review traced declared/effective path pairing, lifecycle terminals, compatibility, cleanup and regression behavior; local security review traced filter/config isolation, authorization-before-mutation, process-group control, timeout/cancellation and recovery audit integrity; typed child timeout mapping was hardened during review and no unresolved actionable finding remains in Task 21 H1–H3/M1 scope
    - external blocker: ไม่ retry/circumvent external real-macOS suite เพราะ recorded account usage limit ยังมีผลถึง August 2 เวลา 11:46 Asia/Bangkok; managed-sandbox skips ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency หรือ policy change ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-22-p0-03-review-fixes.md`
  Multi-intent recovery-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 23 P0-03 High multi-intent recovery บน public `createExecutor.execute` และ `recoverWorktree`; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'a dangling patch intent is recovered before a later accepted mutation' src/executor/executor.test.ts` -> `1` test, `0` pass, `1` fail; history จริงเป็น `INTENT(A), INTENT(B), APPLIED(B)` แทน `INTENT(A), APPLIED(A recovered), INTENT(B), APPLIED(B)` จึงยืนยันว่า later accepted mutation ฝัง dangling generation เดิมได้
    - causal history GREEN: recovery สแกน ordered mutating history ทั้ง run/task, ผูก terminal ด้วย exact `(actionId,intentSeq)`, หา earliest dangling boundary, prevalidate action/role/snapshot/replay evidence ทั้ง suffix ก่อน mutation, rollback snapshot แรก แล้ว replay accepted/dangling generations ตาม intent order; causal rejection ถูกคงไว้โดยไม่ replay และ recovered terminal ถูก append เฉพาะ dangling generation หลัง suffix ทั้งก้อนผ่าน
    - failure/identity GREEN: accepted replay ทุก generation ต้องสร้าง exact logged `resultHash`; final authoritative tree ต้องตรง last causal accepted identity; evidence หรือ replay ที่ใช้ไม่ได้จะ rollback boundary เดิม, terminally reject dangling generations และ invalidate accepted suffix ตาม intent order โดยไม่เหลือ active state ที่อ้าง artifact ซึ่งถูกถอน; recovery รอบสองเป็น no-op
    - admission/idempotency GREEN: public executor recovery dangling history ก่อนรับ mutation ใหม่; duplicate `actionId` ถูกแยกด้วย generation sequence ไม่ใช่ชื่ออย่างเดียว; RUN_COMMAND replay อยู่ใต้ network/filesystem containment เดิม และ rollback-before-replay ทำให้ externally visible authoritative artifact effect ปรากฏครั้งเดียว
    - matrix GREEN: ครอบ exact A-crash/B-accepted legacy history, A/B/C ที่มี dangling ก่อนและหลัง accepted RUN_COMMAND, crash-after-intent/crash-after-apply, duplicate action IDs ข้าม generations, causal rejection, missing replay evidence, terminal log order และ restart ซ้ำสองรอบ; `REQ-3.7|DoD#6` -> `4` pass, `0` fail และ full `REQ-3` -> `47` pass, `0` fail
    - full public/wired files: `executor.test.ts` -> `70` tests, `69` pass, `0` fail, `1` explicit external-only skip; wired `DoD#6c/REQ-3.7` ผ่าน public composition และยืนยัน recovered causal terminal order ก่อน later accepted mutation
    - full core/backend/AAL: `pnpm --filter core test` -> `447` tests, `438` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - root workspace gate: `scripts/ci-test-scope.sh push develop` เลือก full suite; web `74/74`, adapters `39/39`, core และ AAL ผ่าน แต่ command จบ non-zero เฉพาะ full console-backend managed-sandbox limitation เดิม: `383` tests, `329` pass, `54` environment failures โดย `53` เป็น `listen EPERM: operation not permitted 127.0.0.1` และอีก `1` เป็น `node:os.uptime()`/`uv_uptime` EPERM; focused production composition ผ่านและไม่พบ Task 24 functional regression
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> pass; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; strict Evidence, full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness review traced generation binding, suffix rollback/replay, append ordering, failure invalidation, duplicate compatibility and repeated recovery; local security review traced log trust, action/role/evidence validation, patch policy, command sandbox containment and fail-closed rollback; no unresolved actionable finding remains in Task 23 High scope
    - external blocker: ไม่ retry/circumvent external real-macOS suite เพราะ recorded P0-02 account usage limit ยังมีผลถึง August 2, 2026 เวลา 11:46 Asia/Bangkok; managed-sandbox skips และ EPERM failures ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency หรือ policy change ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-24-p0-03-multi-intent-fix.md`
  Admission reconciliation-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 25 High เรื่อง mutating admission ที่รับ `actionId` ใหม่บน artifact identity ซึ่งถูก tamper; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context acceptance re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'admission restores the last applied artifact before accepting a distinct mutation' src/executor/executor.test.ts` -> `1` test, `0` pass, `1` fail; หลัง A สำเร็จและถูก tamper การรับ B ยังคืน `applied` แต่ A คง `external tamper\n` จึงพิสูจน์ว่า B seal tamper เข้า `resultHash` ใหม่
    - admission GREEN: public `createExecutor.execute` สร้าง operation control หนึ่งชุดสำหรับ `WRITE_FILE`, `APPLY_PATCH` และ `RUN_COMMAND` แล้วเรียก `recoverWorktree` เสมอก่อน duplicate/preflight/snapshot/`ACTION_INTENT`; `READ_FILE` และ `REQUEST_TOOL` คง non-mutating exception; reconciliation ตรวจ complete run/task intent history กับ latest active non-blob `ACTION_APPLIED.resultHash` และรับ action ใหม่ต่อเมื่อ state coherent และไม่มี dangling intent
    - mismatch/fail-closed GREEN: เมื่อ latest applied identity ไม่ตรง authoritative artifact ระบบ rollback snapshot ของ exact causal intent แล้ว replay accepted/rejected suffix ตาม `intentSeq`, ตรวจทุก accepted `resultHash` และ final identity ก่อน append recovered terminal; evidence/replay/identity failure rollback และ invalidate accepted suffix แบบ terminal ก่อน reject B โดยไม่มี B intent หรือ side effect; timeout/cancellation ระหว่าง admission ใช้ signal/monotonic deadline เดียวและคืน typed reason
    - causal hardening RED/GREEN: local review เพิ่ม legacy probe ที่ซ่อน accepted A จาก old admission, tamper A แล้วสร้าง dangling B snapshot; RED `1` fail เพราะ recovery เชื่อ snapshot ที่ดูด tamper, GREEN ขยาย replay boundary กลับถึง preceding active `ACTION_APPLIED` และ exact multi-intent regression subset -> `5` pass, `0` fail จึงไม่เชื่อ legacy dangling snapshot แทน accepted identity
    - compatibility matrix: ครอบ tracked patch A/tamper/distinct patch B, persistent ignored-root mutation/deletion/addition ก่อน B ชนิด `WRITE_FILE`/`APPLY_PATCH`/`RUN_COMMAND`, no-tamper fast path, ordinary duplicate skip, stale duplicate refusalหลัง restore, pre-cancelทั้งสาม mutator, tiny deadline, reconciliation failureไม่มี B intent/side effect, repeated restart และ causal multi-intent/rejection/reused actionId behavior
    - focused final: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'REQ-3' src/executor/executor.test.ts test/fault-injection.test.ts` -> `60` tests, `60` pass, `0` fail
    - full public/wired files: `executor.test.ts` -> `83` tests, `82` pass, `0` fail, `1` explicit external-only skip; `fault-injection.test.ts` -> `24` tests, `19` pass, `0` fail, `5` explicit external-only skips
    - full core/backend/AAL: `pnpm --filter core test` -> `460` tests, `451` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - root workspace gate: `scripts/ci-test-scope.sh push develop` เลือก full suiteและจบ non-zero เฉพาะ known managed-sandbox console-backend limitation: `listen EPERM: operation not permitted 127.0.0.1` และ `node:os.uptime()`/`uv_uptime` EPERM; focused backend composition และ full core รอบหลัง hardening ผ่าน จึงไม่พบ Task 26 functional regression และไม่ได้อ้าง environment failures เป็น PASS
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> exit `0`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: required local code-review traced admission order, exact identity selection, causal boundary/replay, duplicate compatibility, deadline/cancellation, cleanup, failure invalidation และ restart behavior; review found and fixed the legacy dangling-snapshot trust edge; no unresolved actionable finding remains in Task 25 High scope
    - external blocker: ไม่ retry/circumvent external P0-02 real-macOS suite เพราะ recorded account usage limit ยังมีผลถึง August 2, 2026 เวลา 11:46 Asia/Bangkok; managed-sandbox skips/EPERM ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency หรือ policy change ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-26-p0-03-admission-reconcile-fix.md`
  Full artifact identity-fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 27 High เรื่อง artifact identity/snapshot/rollback ที่ไม่เห็น Git-ignored bytes นอก `offlineDependencyPolicy.persistentOutputRoots`; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context acceptance re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'Task 27/REQ-3\\.6-3\\.8' src/executor/executor.test.ts` -> `1` test, `0` pass, `1` fail; direct ignored `WRITE_FILE` สองค่าได้ `ACTION_APPLIED.resultHash` เดียวกันคือ `54c45d1bdbfa909bc0661904887a656288dd06f6db6a2a8b693b6eaa7d66d120`
    - authoritative identity GREEN: เพิ่ม bounded `authoritative_worktree` mode ใน shared frozen-tree primitive ซึ่งใช้ descriptor-relative `O_NOFOLLOW` shape traversal, regular-file read และ symlink read; ครอบ stable regular files/safe symlinks ทุก tracked/untracked/ignored path ทั้ง role-allowed และ denied โดย exclude แบบ fixed เฉพาะ `.git` กับ `.ai/runs`; deterministic identity ผูก mode/path/bytes/SHA-256/tree/inventory และ reject special node, unsafe symlink, submodule/gitlink, churn หรือ resource breach
    - snapshot/rollback GREEN: pre-intent EvidenceStore manifest รุ่น 2 เก็บ exact sorted path/mode/bytes/SHA-256/content ref สำหรับ domain เดียวกันและ bind `resultHash`; recovery prevalidate manifest กับ content blobs ทั้งหมดก่อน mutation, ลบ current full inventory/prune empty parents, restore add/delete/mutate รวม executable mode/symlink แล้ว recapture ยืนยัน exact hash; legacy Git `snapshotRef` และ persistent-root manifest replay ยังคงรองรับ แต่ intent ใหม่มี `artifactSnapshotRef` และใช้ full manifest เป็น authority
    - admission/result/recovery GREEN: `WRITE_FILE`, `APPLY_PATCH` และ `RUN_COMMAND` ใช้ identity domain เดียวกันที่ admission, post-effect `ACTION_APPLIED.resultHash`, duplicate/recovery และ suffix replay; policy มี finite `maxFiles`, `maxSingleFileBytes`, `maxTotalBytes`, `captureTimeoutMs` กับ shared `AbortSignal`; rollback cleanup ใช้ separate finite core ceiling เพื่อคืน state ได้เมื่อ action เป็นเหตุให้ identity policy bound ถูกเกิน
    - compatibility matrix: public probes ครอบ direct ignored post-effect hash ทั้งสาม mutators, external ignored add/delete/content+mode mutation ก่อน distinct mutator, denied `docs/`, quoted filename, ignored path นอก persistent roots, safe/unsafe symlink, parent-directory swap race, FIFO, file/single/total bounds, cancellation/deadline, crash-after-apply/restart, repeated recovery no-op และ existing multi-intent causality; P0-02 default command capture/two-inventory/exact dependency-output set ไม่ถูกเปลี่ยน
    - review hardening: local correctness/security review พบและแก้ materialize/content-addressing error ที่เดิมอยู่นอก structured identity rejection, symlink parent-swap ที่ต้องอ่าน target ผ่าน descriptor และ legacy `git_visible` tracked-deletion regression จาก refactor; `DoD#4b` isolated กลับมา pass และไม่เหลือ actionable finding ใน Task 27 High scope
    - focused final: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'Task 28' src/executor/executor.test.ts` -> `16` tests, `16` pass, `0` fail; focused abort/materialize/race/post-hash regressions -> `4` pass, `0` fail
    - full core/backend/AAL: `pnpm --filter core test` -> `477` tests, `468` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - root workspace gate: `scripts/ci-test-scope.sh push develop` เลือก full suite; web `74/74`, core `468` pass/`0` fail/`9` external-only skips, AAL `143/143` และ adapters `39/39` ผ่าน แต่ command จบ non-zero เฉพาะ known managed-sandbox console-backend limitation: `listen EPERM: operation not permitted 127.0.0.1` และ `node:os.uptime()`/`uv_uptime` EPERM; focused production composition ผ่านและไม่ได้อ้าง environment failures เป็น PASS
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - external blocker: ไม่ retry/circumvent external P0-02 real-macOS suite เพราะ recorded account usage limit ยังมีผลถึง August 2, 2026 เวลา 11:46 Asia/Bangkok; managed-sandbox skips/EPERM ไม่ถูกอ้างเป็น real-macOS PASS
    - dependencies/viewports: ไม่มี dependency หรือ policy change ใหม่; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-28-p0-03-full-identity-fix.md`
  Symlink-alias fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 29 High เรื่อง lexical write authorization ที่ตาม pre-existing safe symlink เข้า role-denied, golden, `.ai/runs` และ `.git`; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context acceptance re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'Task 30/REQ-3.4/3.6-3.8' src/executor/executor.test.ts` -> `4` tests, `0` pass, `4` fail; subcase `src/docs-link -> ../docs`, `src/run-link -> ../.ai/runs` และ `src/git-link -> ../.git` คืน `applied` แทน `rejected`
    - descriptor-safe boundary GREEN: เพิ่ม mutation primitive ชุดเดียวที่ใช้ fixed `/usr/bin/python3 -I`, เปิด authoritative root/ทุก parent ด้วย descriptor-relative `dir_fd`, `O_NOFOLLOW` และ `fstat`, reject symlink/non-directory ทุก parent กับ final component ที่ action จะตาม, hard-deny `.git`, `.ai/runs` และ `test/golden`, ตรวจ role roots, expected old state และ staged content hash แล้ว revalidate held parent ทันทีก่อนและหลัง commit
    - transaction/recovery GREEN: regular write/delete, explicit safe-symlink restore และ directory-blocker replacement ทำเป็น batch transaction ด้วย same-directory temporary/backup names; failure, cancellation หรือ parent swap ลบ staged effects และ restore backups ผ่าน held descriptors โดยไม่ตาม symlink; snapshot rollback/replay และ legacy persistent restore ใช้ primitive เดียวกัน, prune obsolete empty directories และ preserve valid safe symlink artifacts; legacy intent ที่ไม่มี complete artifact manifest จะ mutate persistent state ผ่าน primitiveได้เฉพาะเมื่อ Git tree เดิมตรงกัน มิฉะนั้น fail closed โดยไม่ใช้ target `git reset`/`clean`
    - APPLY_PATCH GREEN: Git parse/apply ทำเฉพาะ disposable core-owned tree ที่ materialize จาก frozen input; หลัง apply สร้าง exact regular-file operation set สำหรับ add/update/delete/rename พร้อม old hash/mode preconditions และ new bytes แล้ว promote ผ่าน mutation primitive; ไม่มี `git apply` บน target worktree และ target-controlled symlink จึงไม่อยู่ใน Git TOCTOU window; copy/symlink patch ยังคง structured reject
    - WRITE_FILE/RUN_COMMAND GREEN: `WRITE_FILE` ทำ descriptor preflight ก่อน snapshot/`ACTION_INTENT` และ descriptor commit หลัง intent; `RUN_COMMAND` ยัง execute/capture ใน disposable workspace แล้ว promote exact captured regular operations ผ่าน boundary เดียวกัน รวม approved persistent roots; swapped parent ทำให้ whole batch reject โดยไม่มี escaped bytes, partial promoted file หรือ `ACTION_APPLIED`
    - regression matrix: ครอบ Task 29 aliases ไป `docs`, `.ai/runs`, `.git`, golden, nested relative multi-hop, absolute parent, final symlink, WRITE/PATCH/RUN parent swap, patch delete และ multi-file batch, existing rename/copy behavior, no-partial mutation/event, rollback directory restoration, safe-symlink replay preservation และ Task 28 full authoritative identity/recovery
    - focused final: Task 30 -> `14` pass, `0` fail; Task 30 + Task 28 + cancellation regression -> `31` pass, `0` fail; focused REQ-3 -> `90` pass, `0` fail
    - full core/backend/AAL: `pnpm --filter core test` -> `491` tests, `482` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - root workspace gate: `scripts/ci-test-scope.sh push develop` เลือก full suite; web `74/74`, core, AAL `143/143` และ adapters `39/39` ผ่าน แต่ command จบ non-zero เฉพาะ known managed-sandbox console-backend limitation เดิม: full backend `383` tests มี `54` environment failures โดย `53` เป็น `listen EPERM: operation not permitted 127.0.0.1` และ `1` เป็น `node:os.uptime()`/`uv_uptime` EPERM; focused backend composition `11/11` ผ่านและไม่พบ Task 30 functional regression
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - review: local correctness review traced batch atomicity, add/update/delete/rename, cancellation, directory pruning, legacy compatibility, replay and resource cleanup; local security review traced attacker-controlled paths through role authorization, descriptor walk, target final state, Git isolation, staged bytes, hard-denied roots, parent-swap rollback and core-owned temporary cleanup; no unresolved actionable finding remains in Task 29 High scope
    - external blocker: ไม่ retry/circumvent หรืออ้าง PASS สำหรับ P0-02 external real-macOS suite เพราะ recorded cooldown ยังมีผลถึง August 2, 2026 เวลา 11:46 Asia/Bangkok
    - dependencies/viewports: ไม่มี dependency, policy หรือ public API change; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-30-p0-03-symlink-alias-fix.md`
  Descriptor-transaction race fix evidence (awaiting fresh re-review):
    - scope/status: แก้เฉพาะ Task 31 High สองข้อเรื่อง stale final-component overwrite และ backup cleanup ที่ทำ rollback source หาย; Task 3 ยังคง `[ ]` เพื่อรอ independent fresh-context acceptance re-review และไม่ได้เริ่ม P0-04
    - initial RED: `pnpm --filter core exec node --test --test-reporter spec src/executor/mutation-path.test.ts` -> `2` tests, `0` pass, `2` fail ด้วย `Missing expected rejection (MutationPathError)`; concurrent replacement และ concurrent create หลัง full batch prevalidation ถูก overwrite แล้ว transaction resolve สำเร็จ
    - descriptor CAS GREEN: operation ที่มี current artifact ใช้ atomic descriptor-relative no-replace rename ไป sibling quarantine แล้ว inspect exact captured regular hash/mode, symlink target หรือ empty-directory shape; expected absent และ desired install ใช้ exclusive no-replace; installed object กับ held parent ถูก verify และ stale/concurrent final ทำให้ whole batch fail closed โดย rollback operation ก่อนหน้า
    - rollback/cleanup GREEN: rollback ลบ installed/temporary เฉพาะเมื่อ inode และ desired identity ยังตรง, restore backup ด้วย no-replace เท่านั้น และ reverify captured backup ก่อน restore; backup ทุกชิ้นคงอยู่ถึง explicit commit point จากนั้น cleanup แยกเป็น post-commit phase ซึ่งคืน typed `complete`/`residue` outcome และไม่ย้อนเข้า rollback เมื่อ cleanup ล้ม; quarantine อยู่ข้าง worktree บน filesystem เดียวกันแต่นอก authoritative identity
    - recovery/directory GREEN: directory delete ต้องจับ empty directory ที่ operation CAS point จึง reject unmodeled child แทนการลบ; generated recovery-prune regression คืน captured file bytes/path และ unmodeled empty directory exact; public crash/restart regression ยืนยัน failed recovery ไม่มี `ACTION_APPLIED`, retry หลังเอา blocker ออก replay ครั้งเดียว และ recovery รอบถัดไปเป็น no-op
    - regression matrix: focused mutation primitive ครอบ regular/executable, directory, symlink, absent/create/delete, multi-op, concurrent replace/create, safe no-overwrite restore, retained rollback residue และ injected late cleanup failure -> `6` pass, `0` fail; Task 30 parent/symlink/no-partial-event suite -> `14` pass, `0` fail; Task 32 public recovery -> `1` pass, `0` fail; combined focused -> `21` pass, `0` fail
    - review hardening: broad REQ-3 รอบแรกพบ existing deadline subtest คืน `cancelled` แทน `timed_out` เพราะ low-level child abort ถูก map ก่อน authoritative operation checkpoint; หลังแก้ reason precedence isolated cancellation/deadline -> `3` pass, `0` fail และ full core รอบสุดท้ายยืนยันทั้งคู่ผ่าน; local correctness/security review ตรวจ CAS linearization, no-follow/no-overwrite, nested rollback order, backup identity, special-node error continuation, cancellation, quarantine lifecycle และ caller compatibility แล้วไม่พบ actionable finding คงค้าง
    - full core/backend/AAL: `pnpm --filter core test` -> `498` tests, `489` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail; `pnpm --filter aal test` -> `143` pass, `0` fail
    - root workspace gate: `scripts/ci-test-scope.sh push develop` เลือก full suiteและจบ non-zero เฉพาะ known managed-sandbox console-backend limitations เดิม: loopback `listen EPERM: operation not permitted 127.0.0.1` และ `node:os.uptime()`/`uv_uptime` EPERM; web `74/74`, core transaction/recovery, AAL และ adapters ผ่าน และ focused backend composition `11/11` ผ่าน จึงไม่อ้าง environment failure เป็น PASS
    - typecheck/gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; full-tree secret scan และ `git diff --check` -> exit `0`
    - authority: external/root `cmp` -> exit `0`; authority SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - external blocker: ไม่ retry, circumvent หรืออ้าง PASS สำหรับ P0-02 external real-macOS suite เพราะ recorded cooldown ยังมีผลถึง August 3, 2026 เวลา 20:18 Asia/Bangkok
    - dependencies/viewports: ไม่มี dependency, policy หรือ public package export change; viewports n/a
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-32-p0-03-transaction-race-fixes.md`
  Evidence:
    - satisfies: REQ-3.1–REQ-3.11; implementation/remediation evidence is preserved verbatim in Tasks 20, 22, 24, 26, 28, 30 และ 32
    - acceptance: Task 33 verdict `APPROVE_WITH_EXTERNAL_BLOCKER` with Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-03 finding remains
    - focused: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'REQ-3' src/executor/executor.test.ts test/fault-injection.test.ts` -> `91` pass, `0` fail
    - related: mutation primitive -> `6` pass, `0` fail; Task 30/32 regressions -> `15` pass, `0` fail; full core -> `489` pass, `0` fail, `9` explicit external-only skips; backend composition -> `11` pass, `0` fail; AAL -> `143` pass, `0` fail
    - gates: `pnpm typecheck`, `pnpm lint`, and `scripts/check-core-vendor-free.sh` -> pass
    - external blocker: only P0-02 real-macOS verification remains blocked by its recorded cooldown; Task 2 remains `[ ]` and no managed-sandbox skip is claimed as PASS
    - scope: P0-04 was not started; no production/test/spec-authority change, dependency, commit, or push
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-34-p0-03-close.md`

- [x] 4. P0-04 Authenticate evidence at every trust boundary — write forged/missing/tampered evidence faults first, then harden atomic blob publication, add a Node built-in per-run Ed25519 keypair under run state with private mode `0600`, freeze public metadata/fingerprint, reuse the key on recovery, sign canonical gate/evidence metadata, and require dereference/hash/signature verification before state advancement, approval, merge, audit, or completion.
  Satisfies: REQ-4 (all criteria). Depends on: 1. Verify: evidence-store, authenticator, report-integrity, task-loop, merge, and completion tests prove valid recovery with the same key and structured `ESCALATED` failure for every missing/mismatch case without a dependency change.
  Evidence:
    - satisfies: REQ-4.1–REQ-4.13
    - RED: `pnpm --filter core exec node --test --test-reporter spec src/evidence/store.test.ts src/evidence/auth.test.ts src/gates/report-integrity.test.ts` -> `6` tests, `3` pass, `3` fail; the failures discriminated corrupt existing-blob trust and the then-missing authenticator/report-integrity modules
    - primitives: the same evidence-store/authenticator/report-integrity command after implementation -> `13` pass, `0` fail
    - trust boundaries: REQ-4 merge/approval/completion faults -> `3` pass, `0` fail; full merge queue + auto-merge -> `22` pass, `0` fail; GateRunner -> `11` pass, `0` fail; human approval API -> `34` pass, `0` fail, `1` explicit skip; OOB audit -> `9` pass, `0` fail
    - wired faults: forged signed verdict, missing referenced blob, and tampered referenced blob -> `3` pass, `0` fail; each ends in structured `ESCALATED` and never advances to `PASSED`, `REVIEWING`, or `COMPLETED`
    - composition: full core suite -> exit `0`; full AAL -> `143` pass, `0` fail; full console-backend suite -> exit `0`; targeted fusion -> `9` pass, `0` fail; auditor CLI -> `7` pass, `0` fail
    - gates: `pnpm typecheck`, `pnpm lint`, `.ai/bin/check-secrets.sh --all`, `scripts/check-core-vendor-free.sh`, strict Evidence validation, `git diff --check`, and `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> pass (`144` criteria covered, EARS pass)
    - dependency/security: Node built-in `node:crypto` Ed25519 only; no dependency or lockfile change; private key remains outside supplied worktrees at mode `0600`, frozen run metadata at mode `0444`, and symlink redirects fail closed
    - review: code-quality and security self-review found no remaining actionable P0-04 finding after hardening reject/approval verification, no-follow regular-file reads, auth-directory containment, immutable caller bytes, and duplicate-escalation behavior
    - external blocker: P0-02 real-macOS verification remains blocked by its recorded cooldown until 2026-08-03 20:18 Asia/Bangkok; it was not retried, bypassed, or claimed as PASS
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-35-p0-04-evidence-auth.md`
  Task 37 review-fix evidence (awaiting fresh re-review):
    - scope/status: แก้ Critical 1 และ High 1–3 จาก Task 36 เท่านั้น; Task 4 เปิดค้าง `[ ]` รอ independent fresh-context correctness/security review และไม่ได้เริ่ม P0-05
    - exact RED: probe สี่ข้อแยกรันได้ `0` pass, `4` fail — signed green A ยัง merge red B จน `COMPLETED`; deploy approval คืน HTTP 200 โดย verifier calls `0`; OOB ใช้ alternate evidence store แล้วรายงาน no eligible; COMPLETED ที่ไม่มี T1 exit `0` โดยไม่ escalate
    - immutable authorization GREEN: core re-sign report หลัง commit พร้อม `artifactCommitHash`, `baseCommitHash` และหลัง mergeพร้อม `mergedCommitHash`; ตรวจ signed tree/OID/base ที่ auto approval, human approval, queue lease, merge, audit และ completion; direct/queue merge exact immutable OID ไม่ merge mutable branch name; approval/deploy diff refs เป็น content-addressed bytes และ recompute จาก signed OID ก่อนตัดสินใจ
    - deploy GREEN: `/deploy/decision` authenticate package ก่อน `DEPLOY_DECISION`/callback และ `runDeployStage` authenticate ซ้ำก่อน CANARY; failure คืน typed 409/`ESCALATED` ที่ `deploy_approval` หรือ `deploy_stage` และ executor/deploy callback ถูกเรียก `0`
    - OOB GREEN: evidence root derive เพียง `join(dirname(effectiveDbPath), "evidence")`; ไม่มี CLI/global alternate path; missing T1 และ auth/artifact mismatch คืน structured failure, append `ESCALATED{boundary:"oob_audit"}` และ CLI exit `2` โดยไม่ mutate task state
    - exact GREEN: four Task 36 probes -> `4` pass, `0` fail; deploy-stage second-boundary probe -> `1` pass; direct และ queue genuine non-repro regressionsยัง revert/escalate; OOB suite -> `9/9`; merge queue -> `7/7`; Task 35 key/blob/canonical signature controlsยังผ่าน
    - recovery hardening: run recovery ใช้ frozen `run-metadata.json` เป็น signal แทน SQLite existence เพื่อไม่ตี foreign/concurrent lease-only DB เป็น missing-key recovery; existing two lease regressions RED แล้ว GREEN `2/2`; เมื่อ metadata มีอยู่แต่ key หายยัง fail closed ใน authenticator เดิม
    - self-review RED/GREEN: เพิ่ม fault probes สิบข้อสำหรับ final/legacy tree mismatch, missing-verifier typing, task/deploy wait release, stale base tip, queue ref race, deploy first-parent diff, mutated-report re-sign และ explicit endpoint boundary; รัน RED แยกทุกข้อได้ `0/1` ก่อนแก้ และ GREEN รวม `10/10`; queue ใช้ CAS `update-ref` กับ signed base, verifier ไม่ infer deploy จาก id และ final re-sign ตรวจ signature/tree เดิมซ้ำ
    - full suites: core `525` total = `516` pass, `0` fail, `9` explicit external-only skips; console backend `387/387`; AAL `143/143`; adapters `39/39`; console web `74/74`
    - workspace gates: `pnpm typecheck` ผ่านทั้ง `6` projects; `pnpm lint`, `pnpm build`, vendor check และ lessons coverage ผ่าน; strict Evidence, full-tree secret scan, spec trace `144/144` พร้อม EARS lint, authority SHA และ `git diff --check` ผ่าน; guard regression scripts `14/14` ผ่าน; ไม่มี dependency, lockfile หรือ policy changeใหม่
    - local review verdict: ทบทวน correctness/security ที่ artifact/signature flow, base/queue concurrency, approval API/OOB/deploy trust boundaries และ compatibility แล้วแก้ actionable findingsทั้งหมดด้วย RED-first regressions; ไม่พบ actionable Critical/High/Medium/security finding คงค้าง แต่ Task 4 ยังเปิดรอ independent fresh-context review
    - external blocker: ไม่ retry/circumvent หรืออ้าง PASS สำหรับ P0-02 real-macOS suite; recorded cooldown ยังมีผลถึง August 3, 2026 เวลา 20:18 Asia/Bangkok และ Task 2 คง `[ ]`
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-37-p0-04-review-fixes.md`
  Task 39 direct-merge CAS fix evidence (awaiting fresh re-review):
    - scope/status: แก้ Task 38 High เฉพาะ direct-merge consistency; Task 4 คงเปิด `[ ]` รอ fresh-context review, ไม่เริ่ม P0-05, ไม่ commit และไม่ push
    - exact RED: `pnpm --filter core exec node --test --test-reporter spec --test-name-pattern 'Task 39|first parent is not the signed base' src/merge/auto-merge.test.ts src/merge/artifact-binding.test.ts` -> `2` tests, `0` pass, `2` fail; stale implementationยอมรับ merge ที่ first parent ไม่ใช่ signed base และ concurrent direct main advance ยังจบ `COMPLETED`
    - detached/CAS GREEN: direct path prepare `--no-ff` merge ใน detached temporary worktree จาก exact signed base กับ exact signed artifact OID, authenticate topology ก่อน publish แล้วใช้ `git update-ref refs/heads/main <mergeCommit> <signedBase>`; ไม่มี checkout/merge บน mutable main branch
    - interleaving/OID proof: deterministic event-log seam advance main หลัง verification; ยืนยัน injected concurrent OID ต่างจาก signed base, CAS แพ้, ผลเป็น structured `ESCALATED{boundary:"direct_merge_update",code:"artifact_identity_mismatch"}`, `mergeCommit:null`, main หลังจบเท่ากับ concurrent OID เดิม, ไม่มี `EVIDENCE_AUTHORIZED` หรือ `COMPLETED`; valid direct และ queue controls ยืนยัน parent 1 เท่ากับ signed base OID และ parent 2 เท่ากับ signed artifact OID
    - topology binding: `bindGateReportToMerge` และ `verifyMergedArtifactBinding` ตรวจ first parent ของ merge commit เท่ากับ signed `baseCommitHash` พร้อมตรวจ signed artifact parent; forged final report ที่ใช้ concurrent first parent ถูกปฏิเสธทั้งก่อน re-sign และที่ final verification
    - exact GREEN/regression: exact RED command เดิม -> `2` pass, `0` fail; focused artifact/queue/auto-merge -> `29` pass, `0` fail; deploy/OOB/human approval regression -> `55` pass, `0` fail, `1` explicit external-only skip; completion verifier regressionปรับตาม post-CAS merge-result boundary และผ่าน
    - full suites: core `527` total = `518` pass, `0` fail, `9` explicit external-only skips; console backend standalone exit `0` และ root ยืนยัน `387/387`; AAL standalone exit `0`; full root `pnpm test` exit `0`
    - workspace gates: `pnpm typecheck`, `pnpm lint`, `pnpm build`, vendor-free และ lessons coverage ผ่าน; full-tree secret scan, strict Evidence, spec trace `144/144` พร้อม EARS lint, authority byte-compare/SHA `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3` และ `git diff --check` ผ่าน
    - dependency/security/review: ไม่มี dependency, lockfile หรือ policy changeจาก Task 39; exact OIDs ส่งเป็น `execFileSync` argv, temporary detached worktree ถูกถอนหลัง prepare, ref publication เป็น compare-and-swap และ fail closed; code-quality/security review ไม่พบ actionable finding คงค้างหลังแก้ stale comment
    - external blocker: ไม่ retry/circumvent หรืออ้าง PASS สำหรับ P0-02 real-macOS suite; recorded cooldown ยังมีผลถึง August 3, 2026 เวลา 20:18 Asia/Bangkok และ Task 2 คง `[ ]`
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-39-p0-04-direct-merge-cas-fix.md`
  Task 41 closure evidence:
    - acceptance: Task 40 fresh-context correctness/security review verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-04 finding remains
    - satisfies: REQ-4.1–REQ-4.13; valid per-run Ed25519 recovery, canonical report/blob authentication, exact artifact/base/merge binding, and fail-closed approval/merge/audit/deploy/completion boundaries were verified
    - focused: crypto/blob/report/runner -> `33` pass, `0` fail; Task 37 hardening -> `10` pass, `0` fail; Task 39 direct-CAS/topology -> `2` pass, `0` fail; focused merge suites -> `29` pass, `0` fail; Human Plane/deploy/OOB -> `55` pass, `0` fail, `1` explicit external-only loopback smoke skip; auditor CLI -> `9` pass, `0` fail
    - full suites: `pnpm --filter core test` -> `527` total, `518` pass, `0` fail, `9` explicit external-only skips; `pnpm --filter core typecheck` -> pass; `pnpm --filter console-backend typecheck` -> pass
    - trace: `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed
    - scope: closure changed only this checkbox/evidence block and the Task 41 handoff; no production, test, requirements, design, authority, dependency, commit, or push change
    - external blocker: P0-02 real-macOS verification remains blocked until `2026-08-03 20:18 Asia/Bangkok`; Task 2 remains `[ ]`; no retry, circumvent, managed-sandbox skip, or PASS claim was used
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-41-p0-04-close.md`

- [x] 5. P0-05 Run T0 exactly once per implementation/repair iteration — write ordering/state faults first, then run T0 after every implementer/repair action batch including zero-action claims, run T1 once only after same-artifact T0 on READY, exclude diagnostician/probe rounds, preserve deterministic failure/flake behavior, log every tier result, and verify authenticated evidence before advancing.
  Satisfies: REQ-5 (all criteria). Depends on: 2, 4. Verify: loop sequence-table tests cover WORKING, READY, BLOCKED, repair, diagnostic, zero-action, T0 fail, T1 fail, fail-then-pass, deterministic failure, and logged T2/T3 stubs with exact gate call counts.
  Evidence:
    - satisfies: REQ-5.1–REQ-5.10; REQ-4.12 authentication-before-advancement boundary
    - acceptance: Task 45 fresh-context correctness/security review verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-05 finding remains
    - focused: `pnpm --filter core exec node --test --test-reporter spec src/orchestrator/loop.test.ts` -> `6` pass, `0` fail; T3 stub -> `1` pass, `0` fail; T0 evidence-authentication fault -> `1` pass, `0` fail
    - supporting: Task 44 wired P0-05 suite -> `50` pass, `0` fail, `5` explicit external-only skips; full core -> `526` pass, `0` fail, `9` explicit external-only skips; typecheck and lint -> exit `0`
    - verification: `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md` -> exit `0`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; authority SHA-256 `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - scope: closure changed only this Task 5 checkbox/evidence block and the Task 46 handoff; no production, test, requirements, design, authority, policy, dependency, commit, or push change
    - external blocker: P0-02 real-macOS verification remains blocked until `2026-08-03 20:18 Asia/Bangkok`; no retry, circumvent, managed-sandbox skip, or PASS claim was used; Task 2 remains `[ ]`
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-46-p0-05-close.md`

- [x] 6. P0-06 Fence every task loop with a live lease — write two-loop, single-mode, pause, expiry, and ownership-loss faults first, then require one claim/heartbeat/fencing lifecycle for graph and single-task execution, validate TTL against atomic duration, verify ownership before actions/gates, reacquire after pause, stop old fencing tokens, and release on every terminal/error path.
  Satisfies: REQ-6 (all criteria). Depends on: 5. Verify: lease, core loop, and console composition tests run two actual loops against one event database and prove the loser has no proposal/action/gate events while crash, pause-past-TTL, renewal loss, invalid TTL, terminal release, and valid reclaim controls behave deterministically.
  Task 51 closure evidence (local conformance; external blocker preserved):
    - acceptance: Task 50 fresh-context review verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable correctness/security finding remains in the reviewed source after Task 49 hardening
    - verified controls: required `TaskLeaseSession`, atomic owner/token/TTL-fenced append, proposal/action/gate fencing, single/graph lease composition, heartbeat, pause reacquire, terminal release, stale-token rejection, and planner pre-task scope are recorded in Task 50
    - local evidence: Task 50 recorded core lease/loop `22` pass, AAL source/fusion `31` pass, console fusion/lease `11` pass, workspace typecheck (all `6` projects) pass, lint pass, and core `540` pass with `9` explicit external-only skips; no managed-sandbox failure was misreported as a pass
    - external blocker: the required wired console graph/single composition command `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-run-graph.test.ts` remains blocked in this environment at `1` pass, `11` failures, all `listen EPERM: operation not permitted 127.0.0.1` from the Human Plane listener; this is not a skip or pass and requires an authorized environment/CI run
    - closure decision: Task 6 intentionally remains `[ ]`; do not flip until the same wired graph/single composition suite records an exact authorized-environment pass aggregate. P0-02 real-macOS cooldown is also preserved and was not retried.
    - gate: `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md` -> exit `0`
    - trace: `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered; EARS lint passed
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - hygiene: `git diff --check` -> exit `0`; no production/test/requirements/design/authority edits were made for this closure
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-51-p0-06-close.md`
  Evidence:
    - satisfies: REQ-6.1–REQ-6.10
    - acceptance: Task 50 fresh-context review verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; the sole remaining blocker was the wired console graph/single composition suite that the managed sandbox could not run
    - authorized-environment run (2026-08-02 22:42 Asia/Bangkok, outside the managed sandbox): `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-run-graph.test.ts` -> `12` tests, `12` pass, `0` fail, `0` skipped, duration `249399.996625 ms`; the previously blocked aggregate (`1` pass / `11` `listen EPERM: operation not permitted 127.0.0.1`) no longer reproduces, including REQ-4.6 lease claim/contention/concurrent-runner and REQ-6.5 fixture-pair composition rows
    - supporting local evidence (Task 50/51, unchanged source): core lease/loop `22` pass, AAL source/fusion `31` pass, console fusion/lease `11` pass, workspace typecheck all `6` projects pass, lint pass, core `540` pass with `9` explicit external-only skips
    - viewports: n/a — backend lease/loop composition only
    - deviations: none; the closure flips only this checkbox/evidence and adds handoff `handoffs/task-68-p0-06-close.md`; P0-02 real-macOS and P0-08 operator-fixture blockers are untouched by this closure
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-68-p0-06-close.md`

- [x] 7. P0-07 Validate normalized usage and budget arithmetic — write negative, non-finite, overflow, repair-aggregate, and custom-source faults first, then validate finite non-negative cost at AAL and core boundaries, preserve cost on invalid input, accept zero, and keep iteration/cost/active-wallclock backstops independently terminating in structured escalation.
  Satisfies: REQ-7 (all criteria). Depends on: 1. Verify: core budget/orchestrator and relevant AAL source/repair/fusion tests reject `-1`, `NaN`, infinities, and overflow without crediting cost while zero, finite positive usage, and each independent exhaustion control pass.
  Evidence:
    - satisfies: REQ-7.1–REQ-7.8
    - acceptance: handoff `handoffs/task-55-p0-07-acceptance.md` verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-07 correctness/security finding remains
    - focused core: `pnpm --filter core exec node --test --test-reporter spec src/budget/budget.test.ts src/orchestrator/loop.test.ts test/repair-loop.test.ts test/fault-injection.test.ts test/steering-loop.test.ts` -> `57` tests, `52` pass, `0` fail, `5` explicit external-only skips
    - focused AAL: `pnpm --filter aal exec node --test --test-reporter spec src/repair.test.ts src/source.test.ts src/fusion/run.test.ts` -> `41` pass, `0` fail, `0` skip
    - typecheck: `pnpm typecheck` -> all `6` workspace projects passed
    - lint: `pnpm lint` -> `ESLint: No issues found`
    - verification: `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md` -> exit `0`; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `git diff --check` -> exit `0`
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - external blocker: P0-02 real-macOS verification remains blocked until `2026-08-03 20:18 Asia/Bangkok`; no retry, circumvent, managed-sandbox skip, or PASS claim was used; Task 2 remains `[ ]`
    - scope: closure changed only this Task 7 checkbox/evidence block and handoff `handoffs/task-56-p0-07-close.md`; no production, test, requirements, design, authority, policy, dependency, commit, or push change

- [x] 8. P0-08 Operationalize operator-supplied golden truth and paired coverage — write manifest/coverage/fixture faults first, then add a system verifier with a direct CI invocation, copy-only fixture provisioning, unique-AC golden coverage paired with held-out rate, and no truth-generation API; accept fixture bytes only from the operator and keep this task explicitly open/blocked if those bytes are absent.
  Satisfies: REQ-8 (all criteria). Depends on: 2. Verify: `scripts/check-golden-manifests.sh` plus golden, calibration, composition, and script tests cover exact copy, edit/delete/add/manifest tamper, zero/partial/full unique-ID coverage, source/hash reporting, and the missing-operator-fixture blocker.
  Task 61 closure evidence (blocked pending operator-supplied fixture bytes):
    - acceptance: handoff `handoffs/task-60-p0-08-acceptance.md` verdict `APPROVE_WITH_OPERATOR_FIXTURE_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-08 correctness/security finding remains after Task 59 fixes
    - verified controls: deterministic manifest verification, direct CI invocation, copy-only operator bytes, unique in-scope AC paired coverage, no public truth-generation API, read-only golden policy, tamper rejection, and authenticated source/tree/manifest provenance are recorded in Task 60
    - blocker: exact operator-supplied golden fixture bytes are absent from the repository and working tree; `scripts/check-golden-manifests.sh` -> exit `2`, `BLOCKED: operator_golden_fixture_missing (no _MANIFEST.sha256 under configured golden roots)`; production CLI without `--operator-golden-fixture` -> exit `2`, `platform loop run: operator_golden_fixture_missing (pass --operator-golden-fixture <path>)`
    - closure decision: preserve Task 8 as `[ ]` under REQ-8.6; do not fabricate, regenerate, copy, or relabel synthetic/temporary test bytes as operator truth, and do not claim P0-08 or Phase 0 conformance complete
    - verification: Task 60 recorded focused golden/provenance `24` core tests and `5` console tests passing with `0` failures; workspace typecheck (`6` projects), lint, strict Evidence, spec trace (`144` criteria + EARS lint), and diff hygiene passed; managed-sandbox listener `EPERM` failures remain unclaimed as pass
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - scope: closure changed only this Task 61 evidence block and handoff `handoffs/task-61-p0-08-close.md`; no production, test, requirements, design, authority, policy, dependency, fixture bytes, commit, or push change
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-61-p0-08-close.md`
  Evidence:
    - satisfies: REQ-8.1–REQ-8.11
    - acceptance: handoff `handoffs/task-60-p0-08-acceptance.md` verdict `APPROVE_WITH_OPERATOR_FIXTURE_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; the controls were complete and the sole open item was the absent operator input
    - blocker cleared: the operator supplied golden fixture bytes on 2026-08-03 from `/Users/king_developer/Downloads/How to Create and Verify Golden Fixture Bytes/` — four authentication response fixtures plus their `_MANIFEST.sha256` seal
    - provenance (REQ-8.3/8.11): copied byte-for-byte into `test/golden/` with no regeneration or rewrite; `cmp` reports every one of the five files identical to the operator source. The manifest is operator-authored — core neither generated nor re-sealed it. Digests: `_MANIFEST.sha256` `c8ed772a48a425d008309a77ea3342de8cf1e6236d1abda47fe68e9ebc8dba9d`; `login_failure.json` `dcfeee534c1ce49644b1b9d64a03bad479229615ce06208ce95bcccb782cc783`; `login_success.json` `a4baa49ecdc7b473a40979528eb859c384722ca3c247adab2ac666bc4195cb11`; `registration_failure.json` `53580d90f3c094cfd06b2ed92da5fe6e463945c7305d946ec05e49165d53ae14`; `registration_success.json` `8c50cea35d8a80327ef25a9b86893c8a09bb0bc856bac3d5188a1ac7107cc207`
    - verifier GREEN (REQ-8.1/8.2): `scripts/check-golden-manifests.sh` -> exit `0`, `OK: test/golden/_MANIFEST.sha256 (source=test/golden sourceHash=411eb6261001a0f4de498ce401557269ae339f0521bc0a62d7d41c0ee1582388 manifestHash=c8ed772a48a425d008309a77ea3342de8cf1e6236d1abda47fe68e9ebc8dba9d)`. The same script is invoked directly and unconditionally by `.github/workflows/ci.yml` before install/typecheck/lint/tests
    - tamper matrix on the real operator bytes (REQ-8.10), each run against a copy so the provisioned fixture stayed untouched: edit -> exit `1`; delete -> exit `1`; add -> exit `1`; restore -> exit `0`
    - control suites: `pnpm --filter core exec node --test --test-reporter spec src/gates/golden.test.ts src/calibration/calibration.test.ts` -> `24` tests, `24` pass, `0` fail, `0` skipped, covering exact copy, manifest tamper, zero/partial/full unique-AC coverage (REQ-8.7/8.8), held-out rate paired with coverage (REQ-8.9), source/hash reporting, and the absent-fixture blocker; production composition `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-cli.test.ts` -> `11` pass, `0` fail
    - no truth-generation API (REQ-8.4): the repository ships a verifier and a copy-only provisioner only; the sealing command lives in the operator's own documentation, outside the system, so no runtime or agent path can regenerate or weaken golden truth
    - viewports: n/a — fixture verification and gate wiring only
    - deviations: the operator's directory also contained `goal.yaml` and a README that are deliberately NOT part of the golden root — the verifier hashes every file it finds there, so including them fails with `golden_manifest_mismatch` (reproduced and confirmed). Only the four fixtures and the manifest were provisioned. The supplied `goal.yaml` does not yet satisfy `.ai/schemas/goal.schema.json` (its `acceptance_criteria` are nested prose inside `goal:` and `budget` is absent) and is therefore NOT wired as a loop input; that is an operator decision recorded in the handoff, and it does not affect this task's criteria
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-72-p0-08-close.md`

- [x] 9. P0-09 Enforce prohibited changes without semantic regex theater — write paired syntax/provenance faults first, then move deterministic convention syntax into versioned policy, freeze core-observed RED artifact hashes with provenance, deny implementer write/patch/command mutation of frozen tests, and require a test-designer correction to produce a new observed RED result before refreezing.
  Satisfies: REQ-9 (all criteria). Depends on: 2, 3. Verify: convention and wired executor faults cover whitespace focus/skip, configured bypass/disable/unexplained-ignore syntax, allowed documented controls, frozen-test edit/delete across all action surfaces, and the legitimate test-designer re-RED flow without any semantic classifier.
  Task 66 closure evidence (blocked pending authorized full-suite verification):
    - acceptance: handoff `handoffs/task-65-p0-09-acceptance.md` verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; no actionable P0-09 source finding remains after Task 64 fixes
    - verified controls: versioned convention policy bytes and per-rule documented controls are wired into production GateRunner callers and `gateConfigHash`; authenticated core-observed RED provenance is rehydrated with content/report/current-artifact hash checks; frozen RED mutation is denied through `WRITE_FILE`, `APPLY_PATCH`, and `RUN_COMMAND`, including normalized `..` aliases; test-designer correction requires a new core-observed RED result; no semantic classifier is used
    - local evidence: Task 65 recorded P0-09 focused core `14` tests passed, `0` failed, `0` skipped; focused console `15` passed, `0` failed; full AAL `148` passed, `0` failed, `0` skipped; workspace typecheck (all `6` projects) passed; lint reported `ESLint: No issues found`
    - external blocker: Task 65 recorded `pnpm --filter console-backend test` -> `394` total, `338` passed, `56` failed, `0` skipped; every failure was managed-sandbox Human Plane listener `listen EPERM: operation not permitted 127.0.0.1`, so no full-console product PASS is claimed. The full core run was interrupted before an aggregate result, so no full-core PASS/FAIL claim is made and no external retry is performed in this closure.
    - closure decision: preserve Task 9 as `[ ]`; this is blocked closure evidence only and does not authorize a normal `Evidence:` block, checkbox flip, P0-09 completion claim, or Phase 0 closure claim
    - preserved blockers: Task 2's recorded real-macOS verification blocker and Task 8's missing operator-supplied golden fixture remain unchanged; this closure does not retry, circumvent, fabricate, or relabel either blocker
    - scope: closure changes only this Task 66 evidence block and handoff `handoffs/task-66-p0-09-close.md`; no production, test, requirements, design, authority, dependency, fixture, commit, or push change
  Evidence:
    - satisfies: REQ-9.1–REQ-9.8
    - acceptance: handoff `handoffs/task-65-p0-09-acceptance.md` verdict `APPROVE_WITH_EXTERNAL_BLOCKER`; Critical `0`, High `0`, Medium `0`, Low `0`; the sole open item was the full-suite aggregate Task 65 could not complete, not a source finding
    - blocker cleared: both aggregates now recorded outside the managed sandbox — `PHASE0_REAL_MACOS_TESTS=1 pnpm --filter core test` -> `576` tests, `576` pass, `0` fail, `0` skipped; `pnpm --filter console-backend test` -> `394` tests, `394` pass, `0` fail, `0` skipped. The `56` `listen EPERM: operation not permitted 127.0.0.1` failures Task 65 recorded were a managed-sandbox listener artifact and do not reproduce
    - focused: convention policy + gate wiring -> `5` pass, `0` fail; production console composition `src/loop-cli.test.ts` -> `11` pass, `0` fail
    - verified controls (unchanged since Task 64, re-run green): versioned convention policy bytes and per-rule documented controls wired into production `GateRunner` callers and `gateConfigHash`; authenticated core-observed RED provenance rehydrated with content/report/current-artifact hash checks; frozen RED mutation denied through `WRITE_FILE`, `APPLY_PATCH`, and `RUN_COMMAND` including normalized `..` aliases; test-designer correction requires a new core-observed RED result; no semantic classifier
    - gates: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> pass; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed
    - authority: `cmp` -> exit `0`; SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - viewports: n/a — gate/policy enforcement only
    - deviations: none; P0-09 source was not modified for this closure, only re-verified under the now-unblocked environment
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-70-p0-09-close.md`

- [x] 10. P0-10 Close Phase 0 conformance on wired production paths — run the full DoD 1–9 fault matrix including regression guarantees for existing passes, update only misleading authority references in touched Phase 0 files, add the direct golden CI gate, prove vendor neutrality and L3/L4 human control, then record exact external macOS sandbox and enforcement-floor evidence; do not claim conformance while an operator golden fixture or any gate is blocked.
  Satisfies: REQ-10 (all criteria). Depends on: 2, 3, 4, 5, 6, 7, 8, 9. Verify: exact source comparison/hash, targeted wired fault suite, full core/AAL/console tests, all three typechecks, lint, vendor scan, direct golden verifier, spec trace, and `scripts/ci-test-scope.sh push develop` all pass; live DoD 3 denial runs outside the nested managed sandbox or on CI `macos-latest`.
  Task 67 audit evidence (blocked closure record; checkbox intentionally remains `[ ]`):
    - audit scope: read the pinned authority, approved requirements/design/tasks, and every Phase 0 handoff through `handoffs/task-66-p0-09-close.md`; reconciled the filesystem and current task checkboxes before trusting historical evidence
    - task status: Tasks 1, 3, 4, 5, and 7 are `[x]`; Tasks 2, 6, 8, 9, and 10 remain `[ ]`; no Phase 0 conformance claim is authorized
    - authority: `if cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md; then printf 'cmp_exit=0\\n'; else printf 'cmp_exit=%s\\n' "$?"; fi; shasum -a 256 loop-engineering-implementation-spec.md` -> `cmp_exit=0`; SHA-256 `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - wired DoD matrix: `pnpm --filter core exec node --test --test-reporter spec test/fault-injection.test.ts` -> `28` total, `23` pass, `0` fail, `5` explicit external-only skips; skips are not real-macOS evidence and cover DoD#2c/2d/3/3b/3c
    - Task 6 wired composition: `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-run-graph.test.ts` -> `12` total, `1` pass, `11` fail, `0` skipped; every failure is managed-sandbox `listen EPERM: operation not permitted 127.0.0.1`, so no authorized lease-composition PASS is claimed
    - enforcement floor: `pnpm typecheck` -> all `6` workspace projects passed; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> vendor-free; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `.ai/bin/check-evidence.sh --strict < .ai/specs/loop-engineering-phase0-conformance/tasks.md` -> exit `0`; `git diff --check` -> exit `0`
    - golden/CI scope: `scripts/check-golden-manifests.sh` -> exit `2`, `BLOCKED: operator_golden_fixture_missing (no _MANIFEST.sha256 under configured golden roots)`; `CI_SCOPE_DRY_RUN=1 scripts/ci-test-scope.sh push develop` -> `decision=full`, `DRY_RUN: would run pnpm test`; `.github/workflows/ci.yml` directly invokes the golden verifier before install/typecheck/lint/tests
    - unresolved external gates: P0-02 real-macOS verification remains on the recorded cooldown until `2026-08-03 20:18 Asia/Bangkok`; Task 8 lacks exact operator-supplied golden bytes; Task 9 handoff 66 records full console `394` total / `338` pass / `56` listener `EPERM` failures and a full-core run interrupted before aggregate, so neither full-console nor full-core verification is complete
    - authority-reference hygiene: touched Phase 0 paths still contain legacy `unified-platform-spec.md` references (`.github/workflows/ci.yml:29`, `core/test/fault-injection.test.ts:1`, `scripts/check-core-vendor-free.sh:4`, `core/src/types.ts:1`, `aal/src/protocol.ts:1`); these were not edited during this read-only audit and require owner review under REQ-10.10
    - closure decision: preserve Task 10 as `[ ]`; do not fabricate operator truth, retry the real-macOS run, treat managed-sandbox skips/EPERM as PASS, or claim Phase 0 conformance while any listed gate is blocked
  Task 71 evidence (blocked closure record; checkbox intentionally remains `[ ]`):
    - status change since Task 67: Tasks 1–7 and 9 are now `[x]`; only Task 8 (operator golden fixture) and this task remain `[ ]`. Task 67's three unresolved verification gates — the P0-02 real-macOS run, the Task 6 wired composition suite, and the Task 9 full aggregates — are all cleared and recorded in their own Evidence blocks
    - full DoD 1–9 fault matrix: `PHASE0_REAL_MACOS_TESTS=1 pnpm --filter core exec node --test --test-reporter spec test/fault-injection.test.ts` -> `28` tests, `28` pass, `0` fail, `0` skipped. Task 67 recorded `23` pass with `5` external-only skips covering DoD#2c/2d/3/3b/3c; those five now execute against real `sandbox-exec` and pass
    - regression guarantee for existing passes: full core `576/576`, full console backend `394/394`, full AAL `148/148`, all `0` fail and `0` skipped; no previously passing scenario regressed while P0-02 was repaired
    - live DoD 3 denial: run outside the nested managed sandbox on this macOS host, per the Verify line's requirement. Independent `/usr/bin/sandbox-exec` probes under the exact emitted profile confirm DNS through mDNSResponder, raw-IP TCP, UDP, and unix-domain socket connect all terminate with `rc=137`
    - external macOS sandbox evidence: `sandbox-exec` delivers only `SIGKILL` — `send-signal` with `SIGXFSZ`/`SIGXCPU`/`SIGPROF`/`SIGSYS`/`SIGABRT` is accepted by the parser but never delivered (the denied syscall still returns `EPERM`). This is why the denied operation class is recorded as `unknown` rather than being inferred, and why denial-observation scope stays `direct_only`
    - direct golden CI gate: `.github/workflows/ci.yml` invokes `scripts/check-golden-manifests.sh` unconditionally before install/typecheck/lint/tests; the macOS platform job now also exports `PHASE0_REAL_MACOS_TESTS=1` so the real-enforcement probes run in the gate instead of skipping silently
    - enforcement floor: `pnpm typecheck` -> all `6` workspace projects pass; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)` (vendor neutrality / INV-7); `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> `144` criteria covered and EARS lint passed; `CI_SCOPE_DRY_RUN=1 scripts/ci-test-scope.sh push develop` -> `decision=full`
    - L3/L4 human control: the wired console composition suite `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-run-graph.test.ts` -> `12/12`, covering per-task approval binding (REQ-4.14) and the kill switch that ends the run `CANCELLED` (REQ-4.15)
    - REQ-10.10 authority-reference hygiene: updated only the two references that were misleading about the Phase 0 DoD authority — `.github/workflows/ci.yml` (platform job comment) and `core/test/fault-injection.test.ts` (suite header, now `loop-engineering-implementation-spec.md §11`). Left `scripts/check-core-vendor-free.sh`, `core/src/types.ts`, and `aal/src/protocol.ts` unchanged: each cites a platform-architecture section (`§1.2`/`§2`, `§6`, `§7.1`) that genuinely lives in `unified-platform-spec.md` and was verified present, so those references are accurate rather than misleading
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; SHA-256 remains `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - blocking gate: `scripts/check-golden-manifests.sh` -> exit `2`, `BLOCKED: operator_golden_fixture_missing (no _MANIFEST.sha256 under configured golden roots)`. Task 8 therefore stays `[ ]`
    - closure decision: preserve Task 10 as `[ ]` and claim no Phase 0 conformance. Every DoD scenario, regression suite, and enforcement-floor gate is green, but REQ-10.12 forbids closure while any gate is blocked, and the operator-supplied golden fixture is the one remaining input. Do not fabricate, regenerate, or relabel synthetic bytes as operator truth
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-71-p0-10-status.md`
  Evidence:
    - satisfies: REQ-10.1–REQ-10.12
    - blocker สุดท้ายปลดแล้ว: operator ส่ง golden fixture bytes เมื่อ 2026-08-03 (ดู Task 8) จึงไม่มี gate ใดเหลือสถานะ blocked อีก
    - DoD 1–9 fault matrix ครบ: `PHASE0_REAL_MACOS_TESTS=1 pnpm --filter core exec node --test --test-reporter spec test/fault-injection.test.ts` -> `28` tests, `28` pass, `0` fail, `0` skipped; DoD#2c/2d/3/3b/3c ที่เคย skip เพราะ nested sandbox รันจริงบน `sandbox-exec` แล้วผ่านทุกข้อ
    - regression guarantee ของ pass เดิม (รันซ้ำหลังวาง fixture): full core `576/576`, full console backend `394/394`, full AAL `148/148` ทั้งหมด `0` fail และ `0` skipped ไม่มี scenario ใดที่เคยผ่านแล้วกลับมาแดง
    - live DoD 3 denial: รันนอก nested managed sandbox บน macOS host จริงตามที่ Verify line กำหนด และ probe อิสระด้วย `/usr/bin/sandbox-exec` ยืนยันว่า DNS ผ่าน mDNSResponder, raw-IP TCP, UDP และ unix-domain socket connect จบด้วย `rc=137` ทุกเส้นทาง
    - external macOS sandbox evidence (REQ-10.11): `sandbox-exec` ส่ง signal ได้เฉพาะ `SIGKILL` — `send-signal` ที่ระบุ `SIGXFSZ`/`SIGXCPU`/`SIGPROF`/`SIGSYS`/`SIGABRT` ถูก parser รับแต่ไม่เคยถูกส่งจริง (syscall ยัง deny คืน `EPERM`) นี่คือเหตุผลที่ operation class ถูกบันทึกเป็น `unknown` แทนการเดา และ denial-observation scope คงเป็น `direct_only`
    - direct golden CI gate (REQ-8.2): `scripts/check-golden-manifests.sh` -> exit `0` พร้อม `sourceHash=411eb6261001a0f4de498ce401557269ae339f0521bc0a62d7d41c0ee1582388`; `.github/workflows/ci.yml` เรียก script นี้ตรง ๆ แบบไม่มีเงื่อนไขก่อน install/typecheck/lint/tests และ macOS platform job export `PHASE0_REAL_MACOS_TESTS=1` แล้ว เพื่อให้ probe ที่ต้องใช้ enforcement จริงรันใน gate แทนการ skip เงียบ
    - enforcement floor: `pnpm typecheck` -> ผ่านทั้ง `6` workspace projects; `pnpm lint` -> `ESLint: No issues found`; `scripts/check-core-vendor-free.sh` -> `OK: core/ and aal/ are vendor-name-free (INV-7)` ซึ่งเป็นหลักฐาน vendor neutrality ตาม INV-7; `scripts/spec-trace.sh loop-engineering-phase0-conformance` -> ครอบ `144` criteria และ EARS lint ผ่าน; `CI_SCOPE_DRY_RUN=1 scripts/ci-test-scope.sh push develop` -> `decision=full`
    - L3/L4 human control: `pnpm --filter console-backend exec node --test --test-reporter spec src/loop-run-graph.test.ts` -> `12/12` ครอบ approval decision ที่ผูกกับ task ที่มันระบุชื่อ (REQ-4.14) และ kill switch ที่หยุด driver แล้วจบ run เป็น `CANCELLED` (REQ-4.15)
    - authority-reference hygiene (REQ-10.10): แก้เฉพาะสองจุดที่ชี้ authority ของ Phase 0 DoD ผิด คือ `.github/workflows/ci.yml` (comment ของ platform job) และ `core/test/fault-injection.test.ts` (header ของ suite ซึ่งตอนนี้ชี้ `loop-engineering-implementation-spec.md §11`) ส่วน `scripts/check-core-vendor-free.sh`, `core/src/types.ts` และ `aal/src/protocol.ts` คงไว้ตามเดิมเพราะแต่ละไฟล์อ้าง section ของสถาปัตยกรรม platform (`§1.2`/`§2`, `§6`, `§7.1`) ที่มีอยู่จริงใน `unified-platform-spec.md` และตรวจสอบแล้วว่าตรง จึงเป็นการอ้างที่ถูกต้อง ไม่ใช่ misleading
    - authority: `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md` -> exit `0`; SHA-256 ยังเป็น `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
    - viewports: n/a — เป็น control-plane behavior และ gate wiring ล้วน ไม่มีส่วน UI
    - deviations: none — ข้อค้างเดิมเรื่อง end-to-end run ปิดแล้วใน follow-up ด้านล่าง
    - follow-up (2026-08-03, หลังปิด task): operator ส่ง `goal.yaml` ฉบับตัดสินครบ (golden 4/6 = AC-1..4, budget มาแล้ว) จึงแปลงเป็น `.ai/goals/auth-test-loop-01.yaml` ให้ผ่าน `.ai/schemas/goal.schema.json` (mapping บันทึกใน comment ของไฟล์) แล้วรัน `platform loop run --goal .ai/goals/auth-test-loop-01.yaml --operator-golden-fixture test/golden` (stub adapter) -> `goal auth-test-loop-01 -> REVIEWING (1 iterations)` พร้อม provenance `manifestHash=c8ed772a…dba9d attribution=operator-supplied`; run แรก expose bug จริงใน `core/src/gates/golden.ts` — `canonicalManifest` เรียงบรรทัด manifest ตาม digest (เพราะ `.sort()` หลัง map ที่ hash นำหน้าบรรทัด) จึง reject manifest มาตรฐานแบบ `shasum`/name-sorted ของ operator ทั้งที่ `scripts/check-golden-manifests.sh` ยอมรับ; แก้ให้เรียงตาม relative path (ตรงกับ script และ `LC_ALL=C`) + เพิ่ม regression test ที่บังคับ hash order สวน name order; รันซ้ำ: full core `577/577`, full console backend `394/394`, typecheck `6` projects, lint ผ่าน — handoff: `handoffs/task-74-goal-e2e.md`
    - handoff: `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-73-p0-10-close.md`

## Suggested Execution Order

```text
P0-01
  ├─> P0-02 ──> P0-05 ──> P0-06
  │       ├─> P0-08
  │       └───────────────> P0-09
  ├─> P0-03 ──────────────> P0-09
  ├─> P0-04 ──> P0-05
  └─> P0-07

P0-02..P0-09 ──> P0-10
```

งานนี้ coupled และหลาย task แตะ shared primitives จึงควรรันตาม dependency order
ใน session เดียวหรือ serialize ตาม owner ของ `core/src/orchestrator/loop.ts`;
โดยเฉพาะ P0-05 ต้องเสร็จก่อน P0-06 เพื่อไม่ให้แก้ loop lifecycle พร้อมกัน
