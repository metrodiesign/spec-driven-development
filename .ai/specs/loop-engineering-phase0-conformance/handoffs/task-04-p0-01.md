# Handoff: Task 04 — P0-01 durable normative authority

> From: Codex teammate `/root/p0_01_authority`   To: lead `/root`   Date: 2026-07-27

## Task Summary

Implement P0-01 ของ active spec
`.ai/specs/loop-engineering-phase0-conformance/` โดย pin external normative source
เข้า repository root แบบ byte-for-byte และปิด REQ-1.1–REQ-1.7 โดยไม่แตะ production,
tests หรือ authority รุ่นเก่า

## Current Status

P0-01 เสร็จแล้ว:

- repository-root `loop-engineering-implementation-spec.md` มี bytes ตรงกับ
  `/Users/king_developer/Downloads/loop-engineering-implementation-spec.md`
- `cmp` คืน exit `0`
- SHA-256 ตรง
  `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3`
- task 1 ถูก mark `[x]` พร้อม Evidence ใน edit เดียวกัน
- task 2–10 ยังเป็น `[ ]`
- ไม่มี production source, tests, old master, blueprint หรือ archive ถูกแก้

## Files Changed

- `loop-engineering-implementation-spec.md` — created/untracked — exact byte-for-byte normative authority copy
- `.ai/specs/loop-engineering-phase0-conformance/tasks.md` — edited/untracked — mark task 1 complete and add exact Evidence
- `.ai/specs/loop-engineering-phase0-conformance/handoffs/task-04-p0-01.md` — created/untracked — durable handoff for P0-01

## Important Decisions

- ใช้ verified repository-root copy เป็น sole normative product-behavior authority
  สำหรับ Phase 0 conformance work ต่อจากนี้ เพราะ bytes และ SHA-256 ตรง external source
- Preserve external source และ authority รุ่นเก่าทุกไฟล์โดยไม่แก้
- ไม่รัน production test/typecheck เพราะ task นี้เป็น documentation-only exact-copy
  slice และห้ามแตะ production/test code

## Constraints

- P0-02 เป็นต้นไปต้องอ้าง `loop-engineering-implementation-spec.md` ที่ root เป็น authority
- ห้ามแก้ old master/blueprint/archived platform spec และห้ามใช้เป็น product authority
- ห้าม revert active-spec artifacts/handoffs ของ teammate; repo เป็น shared workspace
- ทุก P0-02..P0-10 task ต้องทำตาม approved requirements/design/tasks และ RED-first
- ห้าม commit/push จนผ่าน review; ห้าม push ตรง main/develop และห้าม force push

## Tests Run

- `cmp -s /Users/king_developer/Downloads/loop-engineering-implementation-spec.md loop-engineering-implementation-spec.md; cmp_status=$?; printf 'cmp_exit=%s\n' "$cmp_status"` -> `cmp_exit=0`
- `shasum -a 256 loop-engineering-implementation-spec.md` -> `ecb0eebf9465b2b68f579b444086eaa755b47514001d31fa5eaee1d3634aecb3  loop-engineering-implementation-spec.md`
- `wc -c -l loop-engineering-implementation-spec.md` -> `345` lines, `41435` bytes
- `git diff --check` -> exit `0`, no output
- `git status --short --untracked-files=all` -> root authority added; active spec/handoffs remain untracked; no old master, blueprint, archive, production, or test path changed
- typecheck: not run — documentation-only exact-copy task; no production code changed
- test: not run — exact `cmp` and SHA-256 are the task's executable acceptance checks

## Known Issues

- ไม่มี issue ภายใน P0-01
- Operator-supplied golden fixture ยังไม่มีตาม task-03 handoff; P0-08 และ P0-10
  ต้องคง open/blocked จนได้รับ bytes จาก operator
- Managed sandbox ยังซ้อน macOS `sandbox-exec` ไม่ได้; P0-10 ต้องใช้ external approval
  run หรือ CI `macos-latest` สำหรับ live egress proof

## Next Recommended Agent

Builder ที่รับผิดชอบ P0-02 shared fail-closed child-process boundary พร้อม tests
RED-first; task นี้แตะ executor/gate security surface จึงควรใช้ high-effort review
หลัง implementation

## Next Steps

1. อ่าน handoff นี้ แล้วรัน `scripts/spec-state.sh loop-engineering-phase0-conformance`
2. รัน `scripts/spec-slice.sh loop-engineering-phase0-conformance 2`
3. Implement P0-02 ทั้ง task แบบ RED-first ตาม REQ-2 และ design โดยไม่เริ่ม P0-03
4. รัน targeted/full verification ที่ task 2 กำหนดและหยุดให้ review ที่ task boundary
