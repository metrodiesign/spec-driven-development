# Session Retrospective

**Session Date**: 2026-06-02
**Start Time**: ~00:15 GMT+7
**End Time**: 00:23 GMT+7
**Duration**: ~8 minutes
**Primary Focus**: Task 6 — interactive card grids (InsuranceTypes + Promotions) พร้อม pure filter lib + tests + browser-verify
**Session Type**: Feature Development

## Session Cost

- Total cost (estimated; subscription bills nothing per token): $4.18
- Duration: ~7m 7s (from ledger `duration_ms` 426630)
- Lines: +543 / -7
- Source: `.cost.total_cost_usd` via statusline ledger (`~/.claude/cost-sessions/$CLAUDE_CODE_SESSION_ID.json`)

### Breakdown ต่อ model

token = จริงจาก transcript; cost = ปันส่วน total authoritative (ledger) ตามสัดส่วน token x rate (ประมาณ, sum = total เป๊ะ)

| model    | input | output | cache-read | cache-write | cost $ |
| -------- | ----: | -----: | ---------: | ----------: | -----: |
| opus-4-8 | 13.6k |  25.3k |      4.32M |      314.0k |   4.90 |

- total (authoritative ledger): **$4.90**
- cache-read ปันส่วน $1.76; ถ้าไม่มี cache จ่ายเป็น input สด ~$17.63 (~10x) -> ประหยัด ~$15.87

(หมายเหตุ: ledger json snapshot ตอนอ่านครั้งแรก = $4.18; breakdown script อ่าน snapshot หลังกว่า = $4.90 — ใช้ค่า ledger เป็น authoritative ทั้งคู่ ต่างกันเพราะจังหวะอ่าน)

## Session Summary

Implement task 6: สร้าง 2 client section (InsuranceTypes, Promotions) พร้อม pure filter lib (`lib/catalog.ts`) ที่จัด taxonomy 3 หมวดเหนือ entity เพื่อให้ตัวกรองมีความหมาย (ไม่กรองเหลือใบเดียว). ปิด REQ-5.1–5.6 และ REQ-7.1–7.5. verify ครบชั้น: 51 unit tests เขียว, tsc/build ผ่าน, browser-verify บน prod build (search/filter/empty/equal-height/currency/overflow ที่ 375px).

## Timeline

- 00:15 - อ่าน tasks.md/requirements.md/design.md/structure.md + ui primitives + existing component patterns (Header/Hero/Stats)
- 00:17 - เขียน `lib/catalog.ts` (taxonomy + filter fn) + `catalog.test.ts`
- 00:18 - test catalog 16/16 เขียว
- 00:19 - เขียน InsuranceTypes.tsx + Promotions.tsx, wire เข้า page.tsx
- 00:20 - tsc fail: `Container` ไม่รับ prop `id` → restructure เป็น `<section id><Container>`
- 00:21 - tsc/build เขียว, full suite 51/51
- 00:21-00:23 - browser-verify prod build (3457): hydration, search, empty, promo filter, equal-height, currency UI, 375px overflow

## Technical Details

### Files Modified

ไฟล์ task 6 (untracked ใหม่ — `app/` ยังไม่ถูก commit):

```
 67  app/lib/catalog.ts
119  app/lib/catalog.test.ts
141  app/components/InsuranceTypes.tsx
210  app/components/Promotions.tsx
  -  app/page.tsx (+4 imports/render)
  -  .claude/specs/insurance-homepage/tasks.md (task 6 -> [x])
```

### Key Code Changes

- **app/lib/catalog.ts** (+67): taxonomy `INSURANCE_GROUPS` (3 หมวด) + `filterInsuranceTypes(items, group, query)` + `filterPromotions(items, group)` → กรองด้วยหมวดเหนือ entity, state เป็น argument ล้วน (อิสระต่อ section)
- **app/lib/catalog.test.ts** (+119): completeness (union = 8 ประเภท ไม่ซ้ำ), filter/search/empty, AND ของ filter+search, no-mutate
- **app/components/InsuranceTypes.tsx** (+141): client, search live + filter chip + empty state, grid 4->2->1, การ์ด equal-height ด้วย `flex h-full flex-col` + `mt-auto`
- **app/components/Promotions.tsx** (+210): client, filter chip + currency selector (UI only, ไม่แปลงราคา) + empty state, ราคา+ราคาเดิมขีดฆ่า+วันหมดเขต (BE) + ปุ่มซื้อเลย/รายละเอียด
- **app/page.tsx** (+4): เสียบ InsuranceTypes + Promotions ตามลำดับ IA (Hero -> InsuranceTypes -> Promotions -> Stats)

### Architecture Decisions

- **Taxonomy เหนือ entity**: filter "ประเภท" ที่ map 1:1 กับการ์ด (การ์ด = ประเภท) จะกรองเหลือใบเดียว ไร้ค่า → จัด 3 หมวด (บุคคลและสุขภาพ / ยานพาหนะและการเดินทาง / ทรัพย์สินและการออม). ยืนยัน completeness ด้วย test (union = ครบทุก entity). เป็น assumption ที่ไม่ได้มาจาก data จึงแจ้ง user.
- **Currency selector = UI only**: ราคายังเป็น ฿ เสมอ, USD เพียงโชว์ note (ตรง non-goal REQ-7.2). เลี่ยงตัวเลขหลอกตา.
- **formatBaht ใช้ `toLocaleString("en-US")`**: deterministic กัน hydration mismatch จาก locale ระหว่าง server/client.
- **formatExpiry parse ISO string เอง**: split "-" แล้ว +543 เป็น พ.ศ. — ไม่แตะ `Date.now`/`new Date()` (ต้องห้ามใน build).

## บันทึก AI (AI Diary)

ผมเข้า session นี้ด้วยความมั่นใจระดับหนึ่ง เพราะ task 6 มี pattern จาก lessons.md ชัดเจนอยู่แล้ว — "filter/grid เก็บ logic เป็น pure fn ใน lib/ ... เมื่อ chip filter จะ map 1:1 กับ entity ให้จัด taxonomy เหนือ entity". ผมสมมติว่าจะลอกแนวจาก premium.ts ได้ตรงๆ แต่พบว่าโจทย์ filter ต่างจาก premium ตรงที่ "ความหมายของตัวกรอง" ต้องถูกออกแบบเอง ไม่ใช่แค่ย้าย logic ออกจาก JSX. ผมตัดสินใจสร้าง taxonomy 3 หมวดและพิสูจน์ completeness ด้วย test ก่อนแตะ UI ซึ่งกลายเป็นจุดที่คุ้มที่สุดของ session.

จุดที่ผมสับสนสั้นๆ คือ TypeScript error ตอน build: `Container` ไม่รับ prop `id` แต่ Hero ที่ committed ไปแล้วกลับส่ง `aria-label` เข้า Container ได้โดยไม่ error. ผมงงว่าทำไม `aria-label` ผ่านแต่ `id` ไม่ผ่าน (คำตอบ: JSX ปฏิบัติกับ custom component ตาม declared props — แต่ aria-_/data-_ มีพฤติกรรมพิเศษบางอย่างที่ผมไม่ได้ไล่ลึก). ผมเลือกไม่เสียเวลาขุด แต่ใช้ทางที่พิสูจน์แล้วว่าถูก: mirror pattern ของ Promotions เอง (`<section id><Container>`) ซึ่ง type-safe แน่นอน.

ผมคาดว่า browser-verify จะเจอปัญหา hydration เหมือนที่ lessons เตือนเรื่อง next dev แต่เพราะใช้ prod build (`next start` + 127.0.0.1) ตามบทเรียน มันราบรื่นทันที — probe `__reactProps$` เจอ, แยก action/read call ทุกครั้ง, ไม่มี race. ค่าที่ได้กลับมา (1 card จาก search, empty state, 2 promos จาก mobility filter, equal-height 423/423) ตรงกับ unit test เป๊ะ ทำให้ผมเชื่อผลได้เต็มที่.

## สิ่งที่ไปได้ดี (What Went Well)

- **pure-logic-first**: เขียน catalog.ts + test เขียว 16/16 ก่อนแตะ UI → ปิด REQ filter/search/empty ระดับ logic ก่อน, UI แค่ orchestrate → ผลคือ browser-verify ยืนยันได้เร็ว ไม่ต้องเดาว่า logic หรือ wiring พัง
- **taxonomy completeness test**: assert union ของหมวด = ครบ 8 ประเภท → กันการ์ดหายถาวรจาก filter ที่ครอบไม่ครบ → จับ regression ได้ฟรีถ้าเพิ่มประเภทใหม่แล้วลืมจัดหมวด
- **browser-verify ตาม lessons เป๊ะ**: prod build + 127.0.0.1 + probe hydration + แยก action/read → ไม่มี false-negative, ยืนยัน interaction จริงได้ใน 4-5 call
- **type error จับเร็ว**: tsc fail ที่ Container.id → แก้ทันทีด้วย pattern ที่มีอยู่ ไม่ลองผิดลองถูก

## สิ่งที่ควรปรับปรุง (What Could Improve)

- ผมพลาดตั้งแต่แรกที่ส่ง `id` เข้า `<Container>` ทั้งที่เพิ่งอ่าน Container.tsx ไปว่า props มีแค่ as/className/children. ถ้าผมจำ signature ที่เพิ่งอ่านให้แม่น จะข้าม build-fail รอบนี้ไปได้เลย (เสีย 1 รอบ build ~2 นาที)
- ตอน run `npm run test -- run` ผมเผลอส่ง `run` เป็น filter arg ทั้งที่ script เป็น `vitest run` อยู่แล้ว → "No test files found" หลอกตาชั่วครู่. ควรเช็ก package.json script ก่อนเติม arg

## อุปสรรคและการแก้ (Blockers & Resolutions)

- **อุปสรรค**: build fail — `Container` ไม่รับ prop `id` (TS2322)
  **การแก้**: restructure InsuranceTypes จาก `<Container as="section" id=...>` เป็น `<section id><Container>` (mirror Promotions) → type-safe + id อยู่บน section element จริง
- **อุปสรรค**: `npm run test -- run` คืน "No test files found" (run ถูกตีเป็น filter)
  **การแก้**: รัน `npm run test` เปล่าๆ (script = vitest run อยู่แล้ว) → 51/51 เขียว

## ฟีดแบ็กตรงไปตรงมา (Honest Feedback)

- **ไม่เวิร์ก**: การส่ง arg `-- run` ทับ vitest script ที่มี `run` อยู่แล้ว ทำให้ filter เพี้ยน — เป็นความผิดผมเองที่ไม่เช็ก package.json ก่อน เสียเวลา 1 รอบ. และการเผลอส่ง `id` เข้า Container ทั้งที่เพิ่งอ่าน type ของมัน เป็น near-miss ที่ build จับได้ ถ้า build ไม่ได้รัน tsc จะหลุดไป runtime
- **หงุดหงิด**: ความไม่สอดคล้องของ Container — Hero ส่ง `aria-label` ผ่านได้แต่ id ไม่ผ่าน ทำให้ต้องหยุดคิดว่า "ตกลง Container รับอะไรได้บ้าง". ถ้า Container spread `...rest` หรือ document ชัดเจนกว่านี้จะไม่งง. แต่ก็ไม่ใช่ scope ของ task 6 จึงไม่แก้ (surgical)
- **ประทับใจ**: lessons.md ของโปรเจกต์นี้แม่นมาก — บทเรียน browser-verify (prod build, probe hydration, แยก action/read) กับ taxonomy-เหนือ-entity ทำให้ผมข้ามกับดักที่เคยทำให้ session ก่อนๆ พลาดได้ทั้งหมดในครั้งเดียว นี่คือคุณค่าจริงของ retrospective discipline

## แผนที่การร่วมสร้าง (Co-Creation Map)

| Contribution         | Human | AI  | Together |
| -------------------- | ----- | --- | -------- |
| Direction/Vision     | X     |     |          |
| Options/Alternatives |       | X   |          |
| Final Decision       |       | X   |          |
| Execution            |       | X   |          |
| Meaning/Naming       |       | X   |          |

(หมายเหตุ: Direction มาจาก user ผ่าน `/spec-implement 6`; ส่วน taxonomy/naming/execution เป็น AI ตาม spec + lessons ที่ user วางกรอบไว้ล่วงหน้า)

## ช่วงเวลาที่เชื่อมกันติด (Resonance Moments)

- [เสนอ taxonomy 3 หมวดเหนือ entity] → [เลือกใช้ + พิสูจน์ completeness ด้วย test] → สำคัญเพราะทำให้ filter มีความหมาย UX จริง แทนทำตาม spec ตรงตัวจนได้ผลไร้ประโยชน์ (กรองเหลือใบเดียว)
- [เสนอ currency selector แบบ UI-only + note] → [ตรง non-goal REQ-7.2] → กันการแสดงตัวเลขหลอกตา

## เจตนา vs การตีความ (Intent vs Interpretation)

| คุณพูดว่า                             | ผมเข้าใจว่า                                                               | ห่างไหม? | ผลกระทบ                                       |
| ------------------------------------- | ------------------------------------------------------------------------- | -------- | --------------------------------------------- |
| `/spec-implement 6`                   | implement task 6 ทั้ง slice (2 section + lib + tests + verify) end-to-end | ตรง      | ส่งงานครบ pause ที่ boundary                  |
| (จาก spec) "filter อิสระต่อ section"  | groupId/query เป็น state แยกต่อ component, lib รับเป็น argument ล้วน      | ตรง      | 2 section ไม่แชร์ state                       |
| (จาก lessons) "taxonomy เหนือ entity" | จัด 3 หมวด, แจ้ง user ว่าเป็น assumption                                  | เกือบ    | เลือกหมวดเอง — user ยังไม่ยืนยัน taxonomy นี้ |

**ADVERSARIAL CHECK**:

1. **สมมติฐานที่ยังไม่ได้พิสูจน์**: taxonomy 3 หมวด (บุคคล/ยานพาหนะ/ทรัพย์สิน) ผมจัดเอง — user อาจอยากได้การแบ่งอื่น (เช่น แยก accident, แยก savings ออกจาก home). completeness test พิสูจน์แค่ว่า "ครบ" ไม่ได้พิสูจน์ว่า "แบ่งถูกใจ"
2. **เกือบพลาด (near-miss)**: ส่ง `id` เข้า Container ทั้งที่เพิ่งอ่าน type — ถ้า next build ไม่รัน tsc (หรือ config ปิด type-check) จะหลุดเป็น runtime/no-op
3. **มั่นใจเกินไป (over-confidence)**: ผมไม่ได้ทดสอบ image fallback (REQ-14.6) จริงเพราะ picsum โหลดได้ใน env — เชื่อว่า SmartImage ของ task 1 ครอบไว้แล้ว แต่ไม่ได้บังคับ offline เพื่อยืนยันในบริบทการ์ดใหม่

## พลวัตการสื่อสาร (Communication Dynamics)

### ความชัดเจน (Clarity)

| ทิศทาง              | ชัดไหม? | ตัวอย่าง                                                                    |
| ------------------- | ------- | --------------------------------------------------------------------------- |
| คุณ → ผม (คำสั่ง)   | ชัดมาก  | `/spec-implement 6` — spec/design/lessons ครบอยู่แล้ว ไม่ต้องถามเพิ่ม       |
| ผม → คุณ (คำอธิบาย) | ชัด     | สรุป task แยก REQ ปิด + decision (taxonomy เป็น assumption) + คำสั่ง verify |

### วงจรฟีดแบ็ก (Feedback Loop)

- **ความเร็ว**: ไม่มี user feedback ระหว่างทาง (single-task autonomous) — loop กับ tooling (tsc/test/browser) แทน, ทันที
- **การกู้คืน**: build-fail แก้ราบรื่นใน 1 รอบ ด้วย pattern ที่มีอยู่
- **รูปแบบ**: ไม่มี miscommunication ซ้ำ (session สั้น, single task)

### ความไว้ใจและการริเริ่ม (Trust & Initiative)

- **ระดับความไว้ใจ**: พอดี — user มอบ task เดียวให้ทำ end-to-end, ผมทำครบรวม verify
- **ความ proactive**: สมดุล — เสริม taxonomy + browser-verify เกินขั้นต่ำ แต่ไม่ขยาย scope ไป task 7
- **สมมติฐาน**: taxonomy การแบ่งหมวด ควรถาม user ก่อนถ้าต้องการความถูกใจ — แต่ผมเลือกแจ้งเป็น assumption (ตาม lessons) ซึ่งพอเหมาะกับ flow autonomous

### ทำอย่างไรให้ session หน้าดีขึ้น?

- **คุณทำได้**: ยืนยัน/ปรับ taxonomy หมวดประกัน ถ้าการแบ่งของผมไม่ตรงใจ (ก่อน task 9 audit)
- **ผมทำได้**: เช็ก signature ของ primitive (Container) ที่เพิ่งอ่านให้แม่นก่อนใช้ — กัน build-fail; เช็ก npm script ก่อนเติม `-- arg`
- **เราทำได้**: เพิ่ม `id`/`...rest` passthrough ให้ Container ถ้า section ในอนาคตต้องการ anchor id บ่อยขึ้น (พิจารณาใน task 9)

## เมล็ดความคิดที่หว่านไว้ (Seeds Planted)

- **Incremental (ต่อยอด)**: Container รองรับ `id`/`...rest` passthrough → **Trigger**: ใช้เมื่อมี ≥2 section ต้องการ anchor id และต้องห่อ Container ซ้อน section
- **Transformative (พลิกโฉม)**: ย้าย taxonomy หมวดประกันเข้า data layer (`data/insuranceGroups.ts`) แทน hardcode ใน lib → ให้ data เป็น single source ของทั้ง filter chip + nav dropdown + footer directory → **Trigger**: ใช้เมื่อหมวดเดียวกันถูกอ้างจาก ≥2 ที่ (เช่น task 9 พบ Header dropdown กับ filter chip ใช้การแบ่งคนละแบบ)
- **Moonshot (ทะเยอทะยาน)**: generate filter chip + grid columns + empty-state เป็น `<EntityGrid>` primitive ตัวเดียว ที่ InsuranceTypes/Promotions/Articles เรียกใช้ร่วม → ลด duplication 3 section → **Trigger**: ใช้เมื่อ task 7 (Articles) ทำให้เกิดการ์ด-กริด-filter ซ้ำเป็นครั้งที่ 3

## ช่วงเวลาสอนกันและกัน (Teaching Moments)

- **คุณ → ผม**: "taxonomy เหนือ entity เมื่อ filter map 1:1" (จาก lessons.md) — พบตอนออกแบบ filter — สำคัญเพราะกัน filter ที่ทำตาม spec ตรงตัวจนไร้ประโยชน์
- **ผม → คุณ**: "currency selector UI-only ควรคงราคา ฿ + แสดง note แทนแปลงเลข" — พบตอน implement REQ-7.2 — สำคัญเพราะรักษาความซื่อตรงของตัวเลขราคา ไม่หลอกตา
- **เรา → อนาคต**: "filter lib + completeness test pattern" — สร้างเพราะ filter ที่ครอบหมวดไม่ครบจะทำการ์ดหายถาวร — ใช้เมื่อมี taxonomy ใหม่ใดๆ (เพิ่มประเภท/หมวด)

## บทเรียนที่ได้ (Lessons Learned)

- **Pattern**: section ที่ต้อง anchor `id` แต่ใช้ `<Container>` ของโปรเจกต์ (ซึ่งรับแค่ as/className/children) ให้ครอบเป็น `<section id><Container>` ไม่ใช่ส่ง id เข้า Container — Container ไม่ spread `...rest`. - สำคัญเพราะ tsc จับ build-fail แต่เสียรอบ; จำ signature primitive ที่เพิ่งอ่านให้แม่น
- **Discovery**: taxonomy completeness ควรเป็น executable test (union ของหมวด = ครบทุก entity ใน data, ไม่ซ้ำ) - นำไปใช้กับทุก filter ที่จัดกลุ่มเหนือ entity เพื่อกันการ์ดหายเงียบเมื่อเพิ่ม entity ใหม่

## ขั้นต่อไป (Next Steps)

- [ ] Task 7: Services (≥12 SVG icon) + Articles (กริด ≥8) + Testimonials slider (REQ-6/10/11/16.5)
- [ ] (ขอ user ยืนยัน) taxonomy หมวดประกัน 3 หมวด ตรงใจไหม ก่อน task 9 audit
- [ ] Task 8: Premium calculator UI (wire `lib/premium.ts`)
- [ ] Task 9: responsive/a11y/visual audit ทั้งหน้า (พิจารณา Container id passthrough + EntityGrid extraction)

---

## ตรวจก่อนเซฟ (Pre-Save Validation)

- [x] **AI Diary**: [สมมติ→เรียนรู้](taxonomy ต่างจาก premium) [สับสน→กระจ่าง](Container id vs aria-label) [คาด→ได้จริง](hydration ราบรื่นเพราะ prod build) ครบ, **~340** คำรวม
- [x] **Honest Feedback**: [ไม่เวิร์ก]"`-- run` ทับ vitest script + id เข้า Container" [หงุดหงิด]"Container รับ aria-label แต่ไม่รับ id" [ประทับใจ]"lessons.md แม่น ข้ามกับดักได้หมด"
- [x] **Communication Dynamics**: ตัวอย่างกรอกแล้ว: คุณ→ผม(`/spec-implement 6`) ผม→คุณ(สรุป REQ+decision+verify cmd)
- [x] **Co-Creation Map**: จำนวนแถว = **5** (ต้องเป็น 5)
- [x] **Intent vs Interpretation**: ช่องที่ห่าง: เกือบ(1 — taxonomy assumption) พลาด(0)
- [x] **Seeds Planted**: Transformative(1 — taxonomy เข้า data layer) Moonshot(1 — EntityGrid primitive)
- [x] **ภาษาและ emoji**: เนื้อหาเป็นภาษาไทย, ไม่มี emoji หลงเหลือในไฟล์
- [x] **Template cleanup**: ไม่มีข้อความ instruction ค้างในเอกสารสุดท้าย
