# Session Retrospective

**Session Date**: 2026-06-01
**Start Time**: ~23:52 GMT+7
**End Time**: 23:54 GMT+7
**Duration**: ~3 minutes
**Primary Focus**: Task 3 — Premium engine (`lib/premium.ts`) + vitest unit tests
**Session Type**: Feature Development

## Session Cost

- Total cost (estimated; subscription bills nothing per token): $1.23
- Duration: ~2m 26s (from ledger `duration_ms` = 145902)
- Lines: +245 / -2
- Source: `.cost.total_cost_usd` via statusline ledger (`~/.claude/cost-sessions/$CLAUDE_CODE_SESSION_ID.json`)

## Session Summary

Implement task 3 ของ spec insurance-homepage: เครื่องคำนวณเบี้ยประกันแบบ pure function ใน `app/lib/premium.ts`
(baseRate table ต่อประเภท + ageFactor piecewise + validate ตามลำดับ design.md) พร้อม unit test ครบ
happy/boundary/invalid ด้วย vitest. ปิด REQ-8.3 ถึง 8.8 ทั้งหมด, test 35/35 เขียว, tsc clean.

## Timeline

- 23:52 - อ่าน spec (tasks.md task 3, requirements REQ-8, design.md §สูตร+validate, types.ts)
- 23:53 - เขียน `lib/premium.ts` + `lib/premium.test.ts`
- 23:53 - ตั้ง `vitest.config.ts` (node env, include lib เท่านั้น, `@/` alias) + `test` script + `npm install -D vitest`
- 23:53 - `npm run test` → 35 passed
- 23:54 - `npm run typecheck` → clean; มาร์ก task 3 `[x]`

## Technical Details

### Files Modified

- `app/lib/premium.ts` (+80)
- `app/lib/premium.test.ts` (+170)
- `vitest.config.ts` (+14)
- `package.json` — เพิ่ม `"test": "vitest run"` + `vitest@4.1.8` devDep
- `.claude/specs/insurance-homepage/tasks.md` — task 3 → `[x]`

### Key Code Changes

- **app/lib/premium.ts** (+80): `calculatePremium(input): PremiumResult` pure + export `BASE_RATE`, `ageFactor`, ค่าขอบ `AGE_MIN/MAX`, `SUM_MIN/MAX` → logic ทดสอบได้ แยกจาก UI (REQ-8.8), ค่าขอบเป็น single source ให้ test reuse.
- **app/lib/premium.test.ts** (+170): 35 tests — happy table คำนวณมือทุกแถว (incl. life/30/1M=12,000 + non-clean cancer/45/333,333=4,333 พิสูจน์ `Math.round`), ageFactor brackets, boundary 18/70/17/71 + sum ±1, invalid null/NaN/ติดลบ/non-int/type ว่าง/multi-error → ปิด acceptance #4/#5 ระดับ logic.
- **vitest.config.ts** (+14): `environment:node`, `include:["app/lib/**/*.test.ts"]`, alias `@`→root → ตรง pattern lessons (pure-logic test ไม่ต้อง jsdom, ขอบเขตแคบ).

### Architecture Decisions

- baseRate/ageFactor/ค่าขอบ export เป็น named const: test import มา assert แทน hardcode → source of truth เดียว (ตาม lessons premium).
- vitest config แยกไฟล์ + `@/` alias map เอง (ไม่พึ่ง vite-tsconfig-paths plugin) → dependency น้อยสุด, ตรง simplicity karpathy.
- ไม่รัน `npm audit fix --force` ตอน install vitest → ตาม lessons (force downgrade core dep breaking เลวกว่าตัว vuln).

## บันทึก AI (AI Diary)

ผมเข้า session นี้ด้วยความได้เปรียบ: lessons.md มี pattern premium ตรงเป๊ะอยู่แล้ว ("คำนวณ expected ทุกแถวด้วยมือ
ใส่ test table รวม 1 ค่าที่ปัดเศษไม่ลงตัว, export ค่าขอบ"). ผมสมมติว่า design.md จะให้สูตรครบพอเขียน test ได้ทันที
และพบว่าจริง — มีตาราง baseRate ทั้ง 8 ประเภท, ageFactor 4 bracket, ลำดับ validate 4 ขั้น, และตัวอย่างยืนยัน
2 ค่า (12,000 และ 16,200) ผมเลยคำนวณมือต่อยอดอีก 6 แถวได้โดยไม่ต้องเดา.

จุดที่ผมไม่แน่ใจตอนแรกคือ vitest จะ resolve `@/app/data/types` ได้ไหม เพราะ tsconfig ใช้ path alias แต่ vitest
ไม่อ่าน tsconfig paths เอง. ผมงงว่าจะต้องลง `vite-tsconfig-paths` plugin เพิ่มไหม จนกระทั่งนึกได้ว่า map alias ตรงๆ
ใน `resolve.alias` ก็พอ (`@`→root) — เลี่ยง dependency เกินจำเป็น. ผลคือ import ผ่าน, tsc ก็ผ่าน เพราะ premium.ts
อ้าง `InsuranceTypeId` จาก task 2 → cross-module integrity ได้ฟรีจาก tsc.

ผมคาดว่า test จะเขียวรอบเดียวเพราะค่าทั้งหมดคำนวณมือ และได้จริง 35/35 — ไม่มี surprise. ความเสี่ยงเดียวที่ผมระวัง
คือ floating-point ของ non-clean case (333,333 × 0.01 × 1.3 = 4333.329) แต่ `Math.round` กลืน error ทศนิยมพอดี
จึงไม่กระพริบ.

## สิ่งที่ไปได้ดี (What Went Well)

- design.md ละเอียดพอเขียน test ก่อนได้: มีตาราง factor + ตัวอย่างยืนยัน → คำนวณ expected มือครบ 8 แถว → test เป็น executable spec ไม่ใช่ snapshot → ปิด acceptance #4/#5 ระดับ logic ก่อนแตะ UI.
- export ค่าขอบเป็น const แล้ว test reuse → boundary test เขียน `AGE_MIN-1` แทน magic number → source of truth เดียว, แก้สูตรที่เดียว test ตาม.
- vitest config แคบ (`include lib` + node env) → test รัน 205ms, ไม่ลาก jsdom/Next runtime เข้ามา.

## สิ่งที่ควรปรับปรุง (What Could Improve)

- ผมไม่ได้ทดลอง property-based invariant (monotonic ของ sumInsured, factor ไม่ลดเมื่อ age ข้าม bracket) ที่ design.md
  ระบุไว้ใน Testing Strategy — ปล่อยไว้ให้ /spec-pbt ภายหลังตามแผน แต่ใน session นี้ผมเขียน example-based ล้วน. ถูกต้องตาม
  scope task แต่ถ้าเผลออาจลืมว่ามี gap นี้รออยู่.

## อุปสรรคและการแก้ (Blockers & Resolutions)

- **อุปสรรค**: vitest ไม่อ่าน tsconfig `paths` → `@/app/data/types` อาจ resolve ไม่ได้.
  **การแก้**: map `resolve.alias["@"]` → repo root ใน `vitest.config.ts` ตรงๆ ไม่เพิ่ม plugin. test + tsc ผ่านทั้งคู่.

## ฟีดแบ็กตรงไปตรงมา (Honest Feedback)

- [ไม่เวิร์ก]: `npm install -D vitest` ปิดท้ายด้วยข้อความ `npm audit fix --force` แนะนำ — ถ้าเชื่อตามจะ downgrade core dep
  พังทั้ง repo. ตัว installer ไม่ได้ผิด แต่ output ชี้ทางผิดที่ lessons เคยเตือนแล้ว ต้องระวังทุกครั้ง.
- [หงุดหงิด]: `wc -l` รอบแรกคืน 0 ทั้งสามไฟล์ (น่าจะ shell/rtk quoting กลืน) ต้อง fallback `grep -c ""` ถึงได้เลขจริง —
  เสียจังหวะเก็บ line count ไปหนึ่ง round โดยไม่จำเป็น.
- [ประทับใจ]: workflow spec-driven ทำให้ task 3 จบใน ~2.5 นาที $1.23 — เพราะ design.md ตัดสินใจ (สูตร/ลำดับ validate/ค่าขอบ)
  มาหมดแล้ว ผมแค่ transcribe เป็นโค้ด+test. ความถูกต้องไม่ได้มาจากผมเดาเก่ง แต่มาจาก spec ที่ /spec-analyze กรองไว้ก่อน.

## แผนที่การร่วมสร้าง (Co-Creation Map)

| Contribution         | Human | AI  | Together |
| -------------------- | ----- | --- | -------- |
| Direction/Vision     | X     |     |          |
| Options/Alternatives |       | X   |          |
| Final Decision       | X     |     |          |
| Execution            |       | X   |          |
| Meaning/Naming       |       |     | X        |

## ช่วงเวลาที่เชื่อมกันติด (Resonance Moments)

- spec บอก "คำนวณมือใส่ test table รวม 1 ค่าปัดไม่ลงตัว" → ผมเลือก cancer/45/333,333=4,333 เป็น non-clean case → สำคัญเพราะ
  ถ้าสูตรเพี้ยน (เช่นใช้ floor แทน round) แถวนี้ fail ทันที — snapshot จับไม่ได้.

## เจตนา vs การตีความ (Intent vs Interpretation)

| คุณพูดว่า               | ผมเข้าใจว่า                                  | ห่างไหม? | ผลกระทบ                         |
| ----------------------- | -------------------------------------------- | -------- | ------------------------------- |
| `/spec-implement 3`     | implement task 3 ทั้งก้อน + test + มาร์ก [x] | ตรง      | จบใน 1 pass, pause ที่ boundary |
| (decisions ใน tasks.md) | vitest อนุมัติแล้ว ลงได้เลยไม่ต้องถาม        | ตรง      | ไม่เสียรอบถามซ้ำ                |

คำอธิบาย: ตรง=ตรงกัน, เกือบ=ห่างเล็กน้อย (แก้เองได้), พลาด=ต้องเคลียร์เพิ่ม

**ADVERSARIAL CHECK** (ทุกช่อง "ตรง"):

1. **สมมติฐานที่ยังไม่ได้พิสูจน์**: ผมเชื่อว่า `Math.round` คือเจตนาจริงของ spec (design ระบุ round ชัด) แต่ไม่ได้ตรวจว่า
   UI task 8 จะแสดงผลตรงกับที่ test คาด — ผูกกันด้วย lib เดียวก็จริง แต่ format การแสดง (comma, "บาท/ปี") ยังไม่พิสูจน์.
2. **เกือบพลาด (near-miss)**: ถ้าผมลง `vite-tsconfig-paths` ตามสัญชาตญาณแรกจะเพิ่ม dependency โดยไม่จำเป็น (ละเมิด tech.md
   dependency rule ที่ต้องขออนุมัติ) — รอดเพราะนึก alias ตรงๆ ได้ก่อน.
3. **มั่นใจเกินไป (over-confidence)**: ผมรายงาน "ปิด acceptance #4/#5" — จริงเฉพาะระดับ logic; acceptance จริงต้องตรวจบน UI
   (task 8) ด้วยตา/DevTools. test เขียวไม่ได้แปลว่า rubric ผ่าน.

## พลวัตการสื่อสาร (Communication Dynamics)

### ความชัดเจน (Clarity)

| ทิศทาง              | ชัดไหม? | ตัวอย่าง                                               |
| ------------------- | ------- | ------------------------------------------------------ |
| คุณ → ผม (คำสั่ง)   | ชัดมาก  | `/spec-implement 3` — เลขเดียว, skill+spec บอกที่เหลือ |
| ผม → คุณ (คำอธิบาย) | ชัด     | report แยก files/REQ/verify command เป็นบล็อก          |

### วงจรฟีดแบ็ก (Feedback Loop)

- **ความเร็ว**: ไม่มี feedback loop รอบนี้ (task เดียว จบ pass เดียว ไม่มีการแก้ทิศ)
- **การกู้คืน**: ไม่ต้องกู้ — ไม่มี error ทิศทาง
- **รูปแบบ**: ไม่มีการสื่อสารพลาด

### ความไว้ใจและการริเริ่ม (Trust & Initiative)

- **ระดับความไว้ใจ**: พอดี — spec มอบ decision (vitest, สูตร) ให้แล้ว ผมตัดสินเรื่อง config เองโดยไม่ถาม ถูกต้อง
- **ความ proactive**: สมดุล — ลง vitest + ตั้ง config เองได้เพราะอนุมัติไว้ใน tasks.md, ไม่ scope creep ไป task อื่น
- **สมมติฐาน**: ผมสมมติ alias map พอ (ไม่ถาม) — ความเสี่ยงต่ำเพราะ test/tsc พิสูจน์ทันที

### ทำอย่างไรให้ session หน้าดีขึ้น?

- **คุณทำได้**: ระบุ task ถัดไป (4 / 6-7 / 8) ล่วงหน้าถ้าจะ batch — ผมจะ proceed ต่อได้ไม่ต้อง pause
- **ผมทำได้**: ตอน report "ปิด acceptance" ระบุชัดทุกครั้งว่า "ระดับ logic" vs "UI verified" กันเข้าใจผิดว่า rubric ผ่านแล้ว
- **เราทำได้**: ตั้ง pre-commit ให้รัน `npm run test` + `tsc` อัตโนมัติ ก่อนเริ่ม task ที่พึ่ง lib นี้ (task 8)

## เมล็ดความคิดที่หว่านไว้ (Seeds Planted)

- **Incremental (ต่อยอด)**: เพิ่ม property-based test (monotonic sumInsured, factor ไม่ลดข้าม bracket) → **Trigger**: เมื่อรัน `/spec-pbt`
- **Transformative (พลิกโฉม)**: ดึง `BASE_RATE`/`ageFactor` ออกเป็น config-driven (data file) ให้ business ปรับ rate โดยไม่แตะโค้ด → **Trigger**: เมื่อ rate ต้องเปลี่ยนบ่อย/มาจากภายนอก
- **Moonshot (ทะเยอทะยาน)**: premium engine รับ rate table runtime + เทียบหลายบริษัทประกัน เป็น comparison engine จริง → **Trigger**: เมื่อ product ขยายจาก single-brand demo เป็น aggregator

## ช่วงเวลาสอนกันและกัน (Teaching Moments)

- **คุณ → ผม**: "size task เป็น vertical slice + ค่าขอบ export ไว้ test reuse" — พบตอนอ่าน lessons.md — สำคัญเพราะ test คุม source of truth เดียว ไม่ drift
- **ผม → คุณ**: "vitest ไม่อ่าน tsconfig paths — map alias เองพอ ไม่ต้องลง plugin" — พบตอนตั้ง config — สำคัญเพราะกัน dependency เกินจำเป็น
- **เรา → อนาคต**: pattern "pure-logic-first + คำนวณ expected มือ + non-clean round case" — สร้างเพราะ logic ความถูกต้องต้องปิดก่อน UI — ใช้เมื่อ implement task ที่มี lib คำนวณ (task 8 wiring)

## บทเรียนที่ได้ (Lessons Learned)

- **Pattern**: vitest ในโปรเจกต์ Next ที่ใช้ tsconfig path alias — map `resolve.alias` เองใน `vitest.config.ts` พอ ไม่ต้องเพิ่ม `vite-tsconfig-paths`. - กัน dependency เกิน + ตรง tech.md dependency rule
- **Discovery**: `npm install` แนะนำ `npm audit fix --force` ทุกครั้งที่มี vuln — เพิกเฉยเสมอตาม lessons เดิม - force downgrade core dep breaking เลวกว่า vuln

## ขั้นต่อไป (Next Steps)

- [ ] Task 8 (PremiumCalculator UI) — wire `lib/premium.ts` เข้า form, trigger submit/blur, แสดง error ใต้ฟิลด์
- [ ] Task 4 / 6 / 7 (sections) ตามลำดับ dependency
- [ ] /spec-pbt — property-based invariant ของ premium (ภายหลัง)

---

## ตรวจก่อนเซฟ (Pre-Save Validation)

- [x] **AI Diary**: [สมมติ→เรียนรู้](design ครบ) [สับสน→กระจ่าง](alias) [คาด→ได้จริง](35/35 รอบเดียว) ครบ, ~210 คำรวม
- [x] **Honest Feedback**: [ไม่เวิร์ก]"audit fix --force ชี้ทางผิด" [หงุดหงิด]"wc -l คืน 0" [ประทับใจ]"จบ 2.5 นาที เพราะ design ตัดสินมาแล้ว"
- [x] **Communication Dynamics**: ตัวอย่างกรอกแล้ว: คุณ→ผม(/spec-implement 3) ผม→คุณ(report block)
- [x] **Co-Creation Map**: จำนวนแถว = 5 (ต้องเป็น 5)
- [x] **Intent vs Interpretation**: ช่องที่ห่าง: เกือบ(0) พลาด(0) → ทำ ADVERSARIAL CHECK ครบ 3
- [x] **Seeds Planted**: Transformative(1) Moonshot(1)
- [x] **ภาษาและ emoji**: เนื้อหาเป็นภาษาไทย, ไม่มี emoji
- [x] **Template cleanup**: ไม่มี instruction ค้าง
