---
description: Create a lean session retrospective at the end of a work session, before clearing context. Captures cost, files changed, and durable lessons.
agent: build
---

Run the spec-retro phase for $ARGUMENTS. Follow .agents/skills/spec-retro/SKILL.md (and the canonical .claude/skills/spec-retro/SKILL.md).

Note: the Session Cost section reads Claude Code's own cost ledger, which does not exist off Claude. When running under OpenCode, record "cost unavailable (ledger not active)" and fill the rest of the template normally.
